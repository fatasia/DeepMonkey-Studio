{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "scene-viewer",
  "description": "Read-only capability set for a bundled single-scene viewer.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
