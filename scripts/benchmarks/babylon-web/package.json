{
  "name": "@bim-studio-benchmarks/babylon-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "V4 性能基准 Babylon Web 轨道采集 harness（独立依赖目录，不进 apps/web 运行依赖）",
  "scripts": {
    "bench": "node run-babylon-web-benchmark.mjs",
    "check": "node --check run-babylon-web-benchmark.mjs && node --check runner/bench.mjs"
  },
  "dependencies": {
    "@babylonjs/core": "9.26.1"
  }
}
