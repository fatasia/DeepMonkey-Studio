# 2026-09-08 最终统一验收

状态：本轮收口范围已完成。行业包扩充、素材新增与模板扩充按用户最新决定暂停，不计入本轮完成口径；`admin/admin`、`.env`、PostgreSQL + MinIO 拓扑及原场景/原模型字节均未修改。

## 交付闭环

- 数据填报：REST 与 PostgreSQL 单记录参数化写回、主键与修订原子校验、409 保稿、提交结果未知时不自动重放、写后强制新读和可操作重试。
- 正式 2D 画布：新增记录表单组件，作者态可配置数据集/记录/字段并真实写回；匿名发布保持只读且不发起记录读写。
- 导出与发布：实际 PDF 双主题/双轮验证保持通过，纸边、操作控件误打印、窄浮层与换行问题已关闭。
- 3D 编辑器：环境编辑时给视口真实让位；仿真运行、轨迹/等待热力、时间线回放与 Study 证据统一；AI 草案可校验后应用到当前 2D 页面并撤销/重做。
- 模型链路：旧 `KHR_materials_pbrSpecularGlossiness` 在导入、优化、预览、场景和 URDF 嵌套 glTF 中按需迁移；现代模型走原快速路径，未知扩展拒绝静默改写，原文件 SHA 保持。
- 素材库：首刷缩略图以解码完成为显式就绪条件，移除导致已解码但未合成的滤镜路径；双主题 1440/980 首帧截图均已绘制。
- 必要性能修补：灯光导引线只在量化端点变化时更新 GPU 几何，已销毁预热任务不再迟到执行；未把专项长稳或全面 WebGPU 重构混入本轮。

## 自动化与运行证据

- `pnpm build`：通过，含 21 个工作区包、Web 生产构建、bundle budget 与 API import smoke。
- `pnpm typecheck`：通过；2194 个源文件全部不超过 800 行。
- Web：438 个测试文件、1827 项通过。
- API：132 个测试文件、597 项通过。
- 正常运行：`pnpm studio check` 为 web 模式健康，API 4100 与 Web 5173 正常。
- 真实浏览器：填报 `dataset-writeback-nw1J0R` 4/4；仿真 `scene-spatial-flow-khNbAy` 4/4；AI `dataset-writeback-mZeRSQ` 4/4；环境/灯光 `light-editing-bWNe3o` 4/4；素材首帧 `source-b-library-om3p2o` 4/4；旧材质原预览 `source-b-review-CAYlIr` 12/12。旧材质完整导入—优化—保存—场景恢复的最终报告以 [旧材质兼容报告](legacy-gltf-material-compatibility-2026-09-08.md) 为准。

浏览器门禁使用隔离项目、临时数据与网络夹具，不修改正常业务数据。钢厂样本存在由驱动产生的 X4122 浮点精度 warning；官方材质转换会对无法裁剪的单色 specular 槽输出精确匹配的 prune warning。两者均被单独记录，未知扩展、产品 warning 与 error 仍会令门禁失败，不能表述为“控制台绝对零 warning”。

## Kimi-95 / 行业对标评分

对标 ThingJS 的素材到场景动线、Unity 的 PBR 与相机一致性、FVS 的画布响应式、山海鲸的场景空气感及西门子的状态/证据纪律。评分只覆盖本轮实际截图和功能范围。

| 维度 | 分数 | 证据 |
|---|---:|---|
| 布局构图 | 9.3 | 1440/980 双主题；环境、仿真面板不再吞没主视口 |
| 令牌一致性 | 9.2 | UI 强调色继续从 `base.css` 的 accent 派生 |
| 排版 | 9.1 | 关键标题/按钮单行，窄窗控件和打印排版已复验 |
| 交互状态 | 9.4 | loading/失败/409/未知结果/只读/撤销均有真实路径 |
| 动效质量 | 9.0 | 缩略图解码后短过渡，场景回放统一由时间线驱动 |
| 3D 渲染 | 9.1 | 旧材质恢复、ACES/PBR 既有管线与双视图真实截图 |
| 信息设计 | 9.2 | 仿真上下文、Study 证据和填报配置渐进呈现 |
| 即时反馈 | 9.4 | 保存、AI 流式、写回、重试和冲突均有明确状态 |
| 响应式与主题 | 9.3 | 暗/浅 1440/980 两轮；重点 980 遮挡与裁切关闭 |
| 语义与文案 | 9.2 | 浏览/预览术语、只读与失败原因保持可操作且克制 |

## 项目级后验收

- 不同 GPU/驱动、任意第三方混合 glTF 扩展与超大共享纹理仍需客户样本矩阵；官方 SG→metal/rough 转换是标准近似，不承诺旧渲染器逐像素一致。
- 行业包、素材审批和模板数量扩充已暂停；恢复时从总账明确重新排期，不重复本轮基础能力。
- [SemaPLC / Astral3D 价值分析](semaplc-astral-value-analysis-2026-09-05.md) 已完成：前者用于 PLC 验证与 Study 证据合同，后者只借鉴转换状态机和资源化流程，不把产品声明当已验证解析能力。
