# GLM → GPT 交接（2026-09-25 晚，收尾快照）

本文件是 GLM 会话结束时的权威交接。接手前先读 `docs/active-task-recovery-ledger.md`（本轮全部条目在 2026-09-25 各批次标题下）与上一份 `docs/handoffs/codex-to-glm-2026-09-25.md`（其中待办已基本消化，本文覆盖其剩余项）。

## 仓库快照

- 分支 `dev-studio`，基线 `5c09e1af` → 本会话末 **65 提交**（全部未 push，遵守不强推纪律）。本会话新增内容以 `git log 5c09e1af..HEAD` 为准，逐文件审查后再动。
- 三套件冻结绿：**API 1580 / Web 4462 / deep-engine 3855**（均全量实跑，0 失败）；`gate:repository`、`audit:licenses`、`gate:artifact-freshness`（新门禁）三绿。
- **工作树有大量未提交文件（约 290 项）**：大部分属并行会话在途改动（`gpuTimer.ts`/`pbrRenderer.ts` GPU 计时、`threeSceneViewer*`、`scenePublicationDependencies.ts`、视图立方等）。**不要清理、不要重置、不要按旧 HEAD 从零开发**；改动归属逐文件核对，多代理用 `git update-index` 精准暂存的先例见总账批次 16。

## 2026-09-25 用户指令时间线（最新优先级以此为准）

1. 最高优先级：**Deep 不依赖 Three + 性能全面超过 Three**（性能已到编排侧极限，剩余 GPU 拖尾等并行会话 gpuTimer 落库后专项）。
2. **烘焙优先补结果持久化和发布一致性** → **已完成**（见下）。
3. **Shader 编译器接成"编辑→预览→绑定对象→保存→发布"正式工具** → 未完成（配额中断），是接手后的第一优先。
4. license 恢复 DMCSL-1.0 → **已完成**（e8772299/fbaee379；注意还有零散消费方可能在 dist 产物里，源码侧已清）。
5. 素材审计用户明确跳过；30 分钟守护取消。

## 本会话已交付（不重建）

### 引擎/编辑器（"Deep 纯编辑"八条路径 1/3/4/5 有实质落地）

1. **公平性能门禁**：`apps/web/scripts/gate-deep-fair-comparison.mjs`——固定位姿+像素确定性守卫+input→submit/GPU p50/95/99+**拖拽平滑度**（相邻帧灰度差→重复帧率）。多轮基线见 `docs/reports/deep-fair-comparison-2026-09-25.md`。**测量纪律：并行负载会污染数据（webgl 参考线自身翻倍即作废），只信安静窗口**。
2. **输入接管（路径 2）**：Deep WebGPU/WASM 模式下视口手势由引擎中立 `DeepCameraController`/`DeepCameraInputSession` 驱动，姿态写回 viewer.camera 单一事实源；OrbitControls 经 intent 层停用；e2e 合同=输入持有画布随后端切换。pointer→submit **15.8→3.8ms**。
3. **拾取 API（路径 3）**：`PbrRenderer.pick`/`pickScene`（CPU Möller–Trumbore）+ 运行包顶层 `objectBindings` 节点映射（native serde 兼容）。**上层未消费**——编辑器点击仍走 Three Raycaster；`PbrRenderer.pick` 的映射接线因 pbrRenderer.ts 属并行会话禁改而未做（pickScene 可直接消费 nodeId）。
4. **辅助图形原生化（路径 4/5 切片）**：选择盒/测量线/gizmo 视觉走 Deep overlay 自绘（`deepOverlayPrimitives.ts`）；**仅 WebGPU 生效**（WASM 无 editorOverlay 通道）；gizmo 拖拽命中/操纵数学仍 Three；angle 测量/剖切盒/标注/灯光代理仍 Three。
5. **命令层（路径 1 地基）**：八条原语（transform/visibility/material/env/lighting/effects/physics/robotPose/deleteSelection）+ applier + **20 处 UI 写点全部走总线**；`EngineTransformAuthoring` 经 SceneTransformGraph 权威通道。设计文档 `docs/specs/engine-neutral-command-layer-design-2026-09-25.md`（含 5 项待批公共合同变更——**实施前需用户批准**）。
6. **性能数据**：WASM 帧节奏四轮打平 WebGL（输入 p95 7.2ms）；拖拽平滑度三后端 1.0（50fps，零重复帧）；黑帧全 0；**唯一未收敛=WebGPU 拖拽 p95 27.8ms**，定性为 GPU 完成队列深度（编排侧已榨干，见报告"提交合并调查"节）。

### 烘焙（用户指定优先级，①②闭合）

- ① UI 接线：`bakeProbeGrid` 成功后 `storeProbeGridBake`（此前断链）。
- ② 服务端持久化+一致性（c4583ba7）：PUT/GET `/api/scenes/:sceneId/probe-bake`（内容寻址 gzip，64MB 上限，每场景 4 份）；Web 回填；服务端候选链三档优先级（显式>持久化>无，失配产物逐位一致）；真实 worker 编译注入验证 64 探针。
- 遗留：浏览器端全链 E2E 未跑（GPU 烘焙段依赖真机 WebGPU）；WASM 查看器消费探针需 Rust 通道（W2⑥）；UI 回填后无法区分"本会话烘焙"与"回填"。

### 治理/发布

- license 恢复 DMCSL-1.0（e8772299/cff38a93/fbaee379：便携 ZIP 打包清单同步）。
- `pnpm gate:artifact-freshness`（bc9730ef）：schema 变更后强制校验 deep-engine dist 与 wasm 二进制新鲜度——**本轮两次"陈旧产物"实锤回归（dist 拒新字段/wasm 拒新包）的机制封堵；runtimePackage/contracts 改字段后必须过此门禁并重建下游产物**。
- Docker：实机验证过；MinIO 官方 2025-10-23 起停止发布社区镜像，compose 用 bitnamilegacy digest 固定快照（数据目录 `/bitnami/minio` 非 `/data`）。
- 桌面三包为 D2 后 native 重建版（EXE 57fe71ed…/NSIS 79c96b86…/MSI f0cecae9…），首帧两轮 0 白闪复录；NSIS 静默卸载因 UAC 无人确认未验证。
- secrets：gitleaks 全历史 1026 提交 0 真实凭据（报告 `docs/reports/secrets-and-asset-license-scan-2026-09-25.md`）。

## 接手后的任务队列（权威排序，总账 8d961b20 有依据）

- **W1② Deep 拾取消费**：编辑器点击在 Deep 模式走 pickScene（`objectBindings` 已透传 nodeId）；发布查看器拾取。注意 pbrRenderer.ts 禁改是否解除（并行会话 gpuTimer 是否已落库）。
- **W1③ 辅助图形补全**：angle 测量/剖切盒/标注/灯光代理原生化 + **WASM overlay 通道（Rust FFI 侧，deep-scene-viewer wasm 需新增 editorOverlay 消费）**。
- **W1④ gizmo 交互迁移**：拖拽命中/操纵数学脱离 TransformControls（视觉已原生化，`deepOverlayPrimitiveSource.ts` 提供 gizmo 输入采集）。
- **W2⑤ Shader 工具链（第一优先，用户指定；代理配额中断未留下源码）**：设计输入在 `apps/web/src/components/ObjectAppearanceEditor.tsx`（现有枚举 shaderEffect 挂载点）+ deep-engine `shaderPackage/{builder,pipeline}` + runtimePackage `shaderPackages` 入口(上限 128)。四切片：编辑 UI+SceneMaterialState 追加可选 `customShader`（允许追加式合同变更）→ 绑定/保存（材质命令 patch 透传零新接线）→ 预览（若消费入口不足，降级为编译诊断+PassCacheKey 展示，不冒充像素预览）→ 发布编译。每切片独立 commit。
- **W2⑥ 烘焙 WASM 查看器消费**（Rust FFI）。
- **W3**：DDGI 动态化上层开关；诊断面板补全（residency/SpatialAA/temporalValidity/clustered 统计）；clustered lighting Native 视觉证据；音频 30min soak 500ms 逃逸插桩；undo 命令逆放；RobotJointPreview 收编（依赖 setRobotPose boolean 语义决策）。
- **UI 技术债（UI 全检代理已记录未修）**：浅色主题 dashboard 检查器硬编码背景；历史裸色值未令牌化；2D 宽高小数溢出。证据 `test-output/ui-consistency-audit-2026-09-25/`。
- **交互/功能全检未完成**：代理两次配额中断，仅留 88 个截图/探针文件（`test-output/interaction-functional-audit-2026-09-25/`，无 report.md）。重跑时按原任务矩阵。

## 已知问题/风险

1. **两次配额中断未留源码**：Shader 工具链与交互全检代理均"exceed quota limit"——接手后优先重跑这两个。
2. 全量负载下 `architecture.test.ts`（30s 超时）与 `industrialFormatWaitingAcceptance.test.ts` 偶发 flake，隔离重跑均过——并行负载敏感，非回归。
3. runtimePackage schema 变更后必须：重建 deep-engine dist + `node scripts/build-wasm-bundle.mjs` + 过 `gate:artifact-freshness`，三者缺一会在运行时才爆。
4. 5 项公共合同变更（命令层设计文档）未批——W2⑤ 的 `customShader` 追加已获功能级授权，但其余（revision 单调化/保存协议等）仍需用户点头。
5. 外发仍 NO-GO：素材许可审计（用户暂跳过）+ 隐私轴新仓路线待用户定时机。

## 快速上手命令

```bash
pnpm gate:repository && pnpm gate:artifact-freshness   # 治理双门禁
node scripts/studio.mjs start web --core-only          # dev(API /api/health 401=存活)
# 公平对比(须安静窗口,dev 起后):cd apps/web && node scripts/gate-deep-fair-comparison.mjs
# wasm 变更后:node scripts/build-wasm-bundle.mjs && pnpm --filter @bim-studio/deep-engine build
```
