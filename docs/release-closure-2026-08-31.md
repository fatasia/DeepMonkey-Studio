# 产品体验与发布闭环报告（2026-08-31）

## 结论

本轮冻结的软件范围已经形成可重复的产品闭环：用户可在同一项目上下文完成 2D、3D、脚本、拓扑、数据接入、AI 问数、设备诊断、物流仿真、轻量工位验证、虚拟调试、保存恢复、预览和发布；WebGL、WebGPU 优先且自动回退、云渲染和只读 Windows 场景客户端使用同一发布模型。

当前版本可以进入公司内部项目替换验证。更准确的表述是：**已覆盖并自动验收的高频数字孪生交付工作流具备替换封闭平台的产品基础**，不是已经证明在全部行业模块、超大真实项目、素材生态或多年现场可靠性上全面超过所有对标产品。性能数据来自固定工业夹具与当前机器，客户模型、现场数据和目标硬件仍需项目级签署。

## 本轮关闭的产品问题

- 2D、3D 与脚本切换回到同一横向任务上下文；脚本草稿在工作区切换和刷新恢复中不丢失，错误可定位、恢复并重新执行。
- 开发流程不再占据每个页面的默认空间：仅在项目场景上下文提供入口，首屏默认收起，点击“展开”才显示步骤详情；资源中心、拓扑和其他页面不重复渲染流程板块。
- 2D 超宽画布按主要业务内容自动聚焦，不再被孤立越界元素或装饰背景压成缩略图；仍保留完整画布检查入口。
- 脚本编辑器优先呈现代码和运行结果，设置、辅助信息与日志渐进展开；多个 JS 文件保持独立模块，项目依赖在安装时锁定并本地缓存，AI 草稿遵循预览、检查、确认和撤销边界。脚本专用 Git 只跟踪脚本，拉取必须在差异确认后才替换并保存。
- 3D 标签支持对象跟随、点击、关闭、会话恢复、碰撞避让与视口边缘裁切；选中标签在拥挤时保留。阴影和精灵透明写深度规则已统一，避免遮挡和重影污染。
- 拓扑闭环覆盖节点创建、连线、属性、保存、刷新恢复和插入看板；2D、3D、脚本、拓扑都进入真实浏览器回归。
- Plant Lite、What-if、工位验证、物流仿真和虚拟调试归入统一 Study：保存权威输入、场景/模型引用、版本、基线谱系、结果指纹、复现入口和 3D 定位。场景存在歧义时不猜测绑定，避免生成伪谱系。
- AskData 已使用真实连接器和数据集走通查询规划、执行、分组聚合、结果表与证据指纹；未知字段、歧义和越权仍由确定性规则阻断。
- 资源治理采用“定义—版本—依赖—实例覆盖—废弃”轻量模型；环境、材质、模型资源可检索、预览、应用、保存和刷新恢复。
- 本地与生产部署均不使用 Docker；本地脚本可一键启动服务和客户端，生产脚本可检查 PostgreSQL 与 MinIO、备份、沙箱恢复和轮换密钥。只读场景客户端的正常浏览不依赖 Python、Node、PostgreSQL 或 MinIO；它使用与普通 Web `dist` 隔离的 viewer-only 产物，不挂载编辑器和作者行为脚本。

## 素材与模板的实际口径

| 类型 | 已验证数量 | 质量口径 |
|---|---:|---|
| 2D 组件与装饰预设 | 156 | 92 个功能组件、64 个装饰预设；具有唯一标识、分类、中文说明、可编辑属性与独立预览 |
| 商业看板模板 | 120 | 12 个行业方向 × 10 个视图；每套生成 9 个可编辑节点，而非静态截图 |
| 3D 工业预制体 | 78 | 覆盖机器人、输送、人员/车辆/AGV、设备、门禁、大屏、围栏、传感器、仓储与公辅；每项具备参数、动作和数据端口 |
| 环境与材质 | 12 套 HDRI、16 套 PBR | 105 个文件共 427.8 MiB；必需贴图、缩略图、文件大小和 SHA-256 完整，通过统一素材库完成应用、保存与刷新恢复 |
| 外部工业模型资产 | 1551 个已审计，1482 个可发布 | 69 个被发布门槛阻断；不把下载数量、结构有效或自动评分等同于全部达到商业成品质量 |

内置可编辑组件、模板和预制体共 354 个。产品门禁会阻断公开页面、模板、素材名和发布产物中的竞品名称或标识。

### 素材质量与首屏验收

全量审计确认 1551 个 GLB 均可完成结构读取，其中 1482 个进入生产目录，69 个保持待复核。缩略图规范化结果为 1495 张合格、56 张不合格；另有 10 个重复内容和 7 个明确的构图/视角问题。上述问题类别存在交叉，因此不能把分类数量直接相加当作阻断总数。极细主体、近空白、有效像素不足、重复内容和人工标记问题不会以“已校验”状态进入产品。

人工复核覆盖精选前排和自动失败样本，用来确认自动阻断方向与实际画面一致；它不是对 1551 项逐一完成人工美术签署。模型 539、621、803、252、802、254、253、273 用于精选前排抽查；614、877、905、1112、1860、305、423、513、944、952、1396、1578 用于失败与人工复核抽查。

真实浏览器门禁同时验证筛选、缩略图、发布状态、导入项目、进入 3D、HDRI 作为背景、PBR 应用到选中模型、保存和刷新恢复。哈希异常、缺少缩略图和废弃资源按设计阻断；故障注入后素材目录仍保留上下文。首屏主操作的位置为：1440×900 下 `844/900px`，1024×768 下 `731/768px`，1024 模板页为 `714/768px`。四项页面审计均为 0 溢出、0 小字号、0 小点击目标。

## 最终自动化证据

本轮在最终代码上分别执行类型、测试、构建、代码体量、品牌隔离、生产产物、素材链路、产品浏览器和在线全链路门禁；各门禁均通过。

| 验收层 | 最终结果 |
|---|---|
| 代码治理 | 1615 个源文件全部不超过 800 行，无豁免；`git diff --check` 无格式错误 |
| 自动化测试 | 2078 项测试通过；另验证 34 个 Node-RED 节点和 18 个 TDengine/Oracle 示例 |
| 构建与包体 | 全仓生产构建和 API 导入通过；主入口 178.5 KiB / 预算 1074.2 KiB，常规 3D 288.2 / 488.3 KiB，按需后处理 3.0 / 214.8 KiB，参数化界面 23.9 / 58.6 KiB，Worker 253.1 / 341.8 KiB |
| 首屏 | 10 个首屏分块，JavaScript 275.4 KiB、gzip 87.1 KiB，低于 1855.5 / 527.3 KiB 预算 |
| 大型按需运行时 | OpenCascade WASM 22431.8 / 24414.1 KiB，IFC 运行时 4028.5 / 4394.5 KiB，IFC Worker 3177.1 / 3418.0 KiB，均不进入普通首屏 |
| 在线工作流 | 30 个业务步骤、16 个页面审计、3 个键盘审计、6 个故障检查全部通过；0 警告、0 控制台错误、0 页面错误、0 请求失败 |
| 素材链路 | 1482 个外部 GLB、12 套 HDRI 和 16 套 PBR 达到发布门槛；真实 GLB 完成检索与导入，HDRI/PBR 完成应用、保存、刷新恢复；缺图、废弃、重复和哈希异常被阻断 |
| 原生运维 | 5/5 原生运维测试通过；真实生产配置的 PostgreSQL/MinIO 预检通过；备份含 1 条数据库状态和 60 个 MinIO 对象，沙箱恢复哈希一致 |

JT reader 类型修复后已在当前共享工作区重新执行 `pnpm build`：21 个可构建工作区全部通过，随后 API 生产包导入 smoke 成功；不是沿用修复前的旧构建结论。

JT 10.3 真实小样本现已贯通 reader → LOD0 GLB → manifest → `/assets` accessor → 3D glTF 浏览链。验收证据为 1 个源网格、1 个实例、6 个面组图元、8 个源顶点、12 个三角面，包围盒 `[0,0,0]`–`[100,80,60]`；GLB 节点的 `ElementId` 与 `properties.json`、`hierarchy.json` 同键，可选中后读取属性。JT 9.5 多网格样本另验证 44 个复用网格、64 个 world-transform 实例、23,999 顶点和 47,962 三角面。能力仅限 9.5/10 小端 TriStrip/TopoMesh v1 子集；没有受支持 LOD0 的同源结构变体仍保持 `waiting_converter` 且不暴露 `geometryUrl`。

JT 与 X_T 聚焦回归 7 个文件、22 项测试通过。2026-09-04 最新完整发布门禁中，API 为 106 个测试文件、410 项，Web 为 312 个测试文件、1102 项，全仓共 2078 项；随后全仓类型检查（含 1615 文件体量门禁）、21 个可构建工作区、Web bundle budget 与 API 生产导入 smoke 全部通过。

在线门禁实际覆盖：登录与项目、2D 拖放、Unity 运行时降级/恢复、脚本错误恢复与跨工作区草稿、GeoJSON 12 个设备方盒和 12 个标签、3D 模型上传/加载/保存、AI 能力目录与 AskData、设备维护诊断、虚拟调试谱系/证据、物流方案对比/复现、拓扑创建/连线/恢复/插入以及发布和匿名公开读取。

## 页面与图形证据

| 场景 | 结果 |
|---|---|
| 2D 1440×900 | 编辑 P95 7.2 ms，运行 P95 7.3 ms |
| 2D 1366×768 | 编辑 P95 7.1 ms，运行 P95 7.2 ms |
| 2D 1024×768 | 编辑 P95 7.6 ms，运行 P95 7.1 ms |
| WebGL 3D | 144 fps，P95 7.1 ms，P99 7.3 ms，258 draw calls，25 次场景循环，0 失败 |
| WebGPU 3D | 144 fps，P95 7.1 ms，P99 7.3 ms，188 draw calls，25 次场景循环，2 次预期的渲染器生命周期回收，0 失败 |
| 双后端固定画面 | SSIM 0.9881，PSNR 33.65 dB |

全部页面审计均未发现溢出、低于门槛的界面文字、小点击目标或视觉失败。固定 3D 夹具的稳态帧性能良好，但首次加载仍记录到主线程压力信号，因此不能把本结果解释为任意客户场景都没有 Long Task，也不能据此宣称与客户端引擎在所有负载下性能相同。WebGL/WebGPU 画面仍有网格、选区与抗锯齿细节差异，门禁证明当前发布质量可接受，不代表像素完全一致。

复核材料位于：

- `test-output/product-browser/report.json`
- `test-output/online-flow/report.json`
- [素材与材质闭环报告](../test-output/asset-material-flow/report.json)
- [1440 素材治理与哈希故障注入](../test-output/asset-material-flow/01-asset-center-governance.png)
- [真实 GLB 应用 HDRI/PBR](../test-output/asset-material-flow/02-applied-in-3d.png)
- [保存刷新后的外观恢复](../test-output/asset-material-flow/03-reloaded-appearance.png)
- [1024 素材首屏](../test-output/asset-material-flow/04-asset-center-1024.png)
- [1024 商业模板预览](../test-output/asset-material-flow/05-template-center-1024.png)
- [1551 个外部模型全量审计](../data/external-assets/source-a/audit.json)
- [HDRI/PBR 完整性目录](../data/external-assets/environment-materials/catalog.json)
- `test-output/online-flow/02b-behavior-editor.png`
- `test-output/online-flow/03-scene-editor-model-loaded.png`
- `test-output/online-flow/03aa-ai-ask-data.png`
- `test-output/online-flow/04a-virtual-commissioning.png`
- `test-output/online-flow/04b-topology-editor.png`
- `test-output/online-flow/05-published-manager.png`

## 发布与部署结论

四种交付路径已经使用同一项目发布模型：

1. WebGL：兼容发布。
2. WebGPU 优先：先真实请求适配器，不支持或初始化失败时稳定回退 WebGL。
3. 云渲染：保留独立交付选项和故障提示。
4. Windows 场景客户端：只包含指定发布版本的浏览场景和可选工具栏，资源随包固化；正常浏览不需要填写服务 IP，也不提供切换在线服务、编辑、保存、脚本、AI 或实时数据配置能力。需要可配置服务地址的在线开发使用完整编辑客户端。

生产环境使用 PostgreSQL 与 MinIO；本地离线开发使用本地存储和可选同步。弱网重试、断线状态、恢复与冲突不能静默覆盖。`pnpm dev:local:services` 已实际拉起 PostgreSQL/MinIO 依赖与 API，随后 `pnpm deploy:cloud:check` 完成数据库查询、对象存储健康与读写删除预检；`--check` 模式在 Windows 终端中会在输出探针结果后正常退出。密钥内容不得进入日志或文档。

## 已接受的 Windows 验收边界

当前宿主未启用 Windows Sandbox，执行进程也没有管理员权限；MSI/NSIS 是 per-machine 包，因此无法在本机真实完成“干净安装→同升级码覆盖升级→配置保留→卸载→故障恢复”。本轮完成了安装包构建、管理镜像解包、包内程序启动、升级元数据与原生配置/恢复自动测试，但没有把这些等同于干净机器生命周期。

仓库仍保留以下可选验收脚本，后续具备环境时可以执行：

```powershell
apps/desktop/scripts/verify-windows-install-lifecycle.ps1
```

脚本要求两个不同版本 MSI，拒绝在已存在产品安装的污染机器上运行，并输出安装、启动、升级、配置保留和卸载证据。2026-08-31 用户明确决定略过该项，因此它不再是本轮发布资格或目标完成条件；报告继续保留“未执行”事实，避免把构建、解包或包内启动冒充为真实安装生命周期。

真实客户资产、客户数据源、现场网络和目标硬件的演练属于具体项目上线资格，也不能用固定夹具冒充。JT、X_T/X_B 等工业格式的真实转换验收、电池模型正式上线以及 PINO/PINN/TwinMoE 生产证据已由用户明确排除，不列为本轮缺口；现有路由和安全阻断保持不退化。

## 最终判定

软件代码、页面、核心流程、素材/模板数量、发布门禁、生产存储预检、备份恢复和文档已完成本轮冻结范围。干净 Windows 管理员生命周期验收已经用户确认略过，不再阻塞内部替换试点；任何客户项目仍须用自己的模型、数据和硬件完成项目级性能与可靠性签署。
