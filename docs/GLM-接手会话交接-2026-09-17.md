# GLM 接手会话交接:冲刺收官 · Deep2D/DE26/工业格式 · 2026-09-17

> **本文件由 GLM 冲刺会话(2026-09-17 晚)产出,是下一会话的第一入口。**
> 用户 ACTIVE GOAL 已注册于 [active-task-recovery-ledger](active-task-recovery-ledger.md) 头部:
> **一天内完成 Deep2D 剩余 + Deep Engine(三维/DE26)+ 工业格式全部可执行任务;被外部前置
> 阻塞的卡逐张列明阻塞物。** 本会话结束时用户指令"停止任务",目标移交给下一会话继续。
> 判断任何项是否完成,以总账最新条目与实际命令为准,不以下文历史为豁免。

## 1. 当前代码基线

- 工作区:D:/Documents/bim/bim-studio;分支:dev-studio
- 本会话 HEAD 序列(全部已验证后提交):`b912d5a`(可信布局宿主)→ `5b75182`(作者端离线包)→ `b4b3466`(C1/C2 浏览器门禁)→ `fed27b3`(工具栏捕获)→ `b3c3839`(P1-22 回放)→ `2eec131`(A01)→ `ad1b076`(B01)→ `cabb1a5`(purity 修复)→ `e0b2568`(A03)→ `43d327d`(A02)→ `ad3e432`(JSON 同步)→ `43af23e`(C01)→ `2d86950`(PLAN-02)→ `722accf`(语料改判)→ `746ce63`/`784f4af`(C02 整树)→ `dd4e63e`(G02 方案)→ 其余簿记
- **工作树遗留(在途,未验证)**:C03 中途停止的改动 —— 21 文件 +206/-54(spec 101 行已成形:`de26-c03-transparency-twosided-2026-09-17.md`,支持矩阵已冻结;TS renderPacket*/materials 侧 + Native contract/pipeline/mesh_pass 侧实现中段)。**门禁一次都没跑**。接手选择:① 续写至门禁全绿后提交;② `git checkout -- <files>` 回退后重做。不要在未跑门禁时当作完成提交。

## 2. 已完成且有实证(按战场)

### 交接文档下一批(4/4,100%)
1. 可信 Chromium 布局宿主:合同 `apps/api/src/dashboardMeasuredLayout.ts`(宿主只回传测量,绑定服务端算)+ 真实浏览器端到端脚本 `scripts/verify-dashboard-trusted-layout-host.mts`(双跑 hash 一致/取消/缺字体四验证)。spec:`dashboard-trusted-layout-host-2026-09-16.md`
2. 作者端离线包入口:`dashboardOfflinePackageState.ts` 纯状态机 + 对话框 + api 三方法(提交 5b75182)。**浏览器截图闭环待补**(重开条件在台账)
3. CSV/Excel 工具栏捕获合同(tool 角色,提交 fed27b3)+ 真实组件接线与 DOM 捕获断言
4. C1 输入矩阵 + C2 整页视觉:门禁 `apps/web/scripts/gate-dashboard-c1-input-matrix.mjs`(15 步真实点击,排序/分页/行点/CSV 下载/换页路由/双主题双宽度)。QA 应用 `d7c63730-393f-4ade-a6ec-60af914d7c0a`(发布 rev3);主题走 branding PATCH 链路

### Deep2D(权威表 docs/specs/deep2d-remaining-tasks-2026-09-16.md)
- **P1-22** 统一固定步回放:`src/replay.rs`+`replay/`(sim/命令/输入/种子化 RNG;双跑逐字节一致;过期命令 submit+settle 双侧 CAS 拒绝;取消终态幂等)。lib 264→275 基线,集成 7 测试
- **P1-23** N1 版本化适配器:`src/adapter_n1.rs`+`adapter_n1/`(SVG 子集/富文本/图表扩展/动画 ABI;认证台账+摘要门禁;Unknown/Blocked 三态 fail-closed;产物入 P1-22 回放双跑一致)。lib 282/0/1
- 剩余:P2-01~04(兼容实验,X 目标隔离→ZRender→中文富文本→认证淘汰报告;N0 铁律不变)、P3-01~05(热同步,复用包 diff/watch/LKG)

### DE26(82 卡清单 docs/specs/deep-engine-execution-tasks-2026-09-16.json,状态已同步)
- **A01 完整**:五引擎目标矩阵 v2(`benchmarkTargetMatrix.ts`;固定分母/排除项不计通过/权重防篡改/unlocked 永不通过)。spec:`de26-a01-target-matrix-v2-2026-09-17.md`
- **A02 第一批**:资产清单合同 v1 + 真实语料(**用户指令:D:/Download 的 BIMFACE 示例 6.2MB + test-model 的 Snowdon 94.7MB;[已脱敏]目录排除**;BIMFACE"(1)"与原件同哈希已去重)。生成器 `scripts/generate-benchmark-asset-manifests.mts`
- **A03 TS 侧**:采样窗口合同(`benchmarkSampleSchema.ts`;8 通道/unavailable 禁伪零)。**native telemetry_gpu 接线未做(cargo 车道当时被占)**
- **B01**:场景变化集(`scene/SceneChangeset.ts`;节点级 lastChangedRevision CAS+hidden 权威态;两阶段应用+同通道撤销)。Three 侧投影消费(B02)未接
- **B03**:计划↔实际对拍层(`pbrFramePlanExecutor.ts` 16 测试;曾因并行文件组暂缓,C02 整树落地时解除)。真实 GPU 提交顺序切换归真机门禁
- **B04**:帧内 transient 纹理池(`pbrTransientTexturePool.ts`;queue 生命周期/history 双排除/fail-closed 边界;未接真实 renderTargets 归真机切片)
- **C01 合同**:能力清单账本(`gltf/capabilityInventory.ts`;可渲染/语义保留分列)。三路径语料 runner 待 RVT→glTF 转换
- **C02**:顶点色+flat 贯通(ABI v4 冻结;颜色流 packet 合同;flat 几何派生;Native color_buffer;**同批吸收并行在途 transparency/residency/bloom 工作并修复其 96 失败+11 类型错**,提交 746ce63/784f4af)。glTF COLOR_0 解码与 shader 采样(像素级)归下一子步
- **A08 第一切片**:确定性证据索引(`scripts/generate-de26-evidence-index.mts`;双跑同哈希;覆盖 7 卡 0 缺失)。源码身份绑定归后续
- **G02 方案冻结 + 切片 1**:筛选器 chrome 编译(`dashboardWidgetContent.ts` filter 分支;选中高亮矢量可编译/字形延迟登记/空选项与截断与父参数禁用边界)。方案:`de26-g02-filter-compile-plan-2026-09-17.md`(切片 2 命中路由→setFilter、切片 3 数据流待做)

### 工业格式
- **PLAN-02 完成**:试构建报告(`industrial-format-plan02-build-trial-2026-09-17.md`):laz-perf 零补丁成功(0.67MB,LAZ 92ms);openNURBS 失败根因(7 文件平台分支缺 MinGW,3141 undefined ref);libE57Format 缺 XercesC;3D Tiles 离线不可装。RVT 深扫 37 个 4.96GB([已脱敏] 28 个**已按用户指令排除**,语料改 D:/Download)
- 前置:PLAN-01 语料锁定、PLAN-04 审计(前会话)。剩余:PLAN-02 MSVC 复测、PLAN-03/05~11

### 门禁修复
- **purity 门禁 HEAD 红转绿**(cabb1a5):类型-only 桥导入豁免+两个 DOM 面允许条目;585 源通过
- **已知仍红**:sourceSizeGate 7 项(全历史文件:chart_render.rs 748 等);修复需拆他人历史文件,列专门切片

## 3. 未完成/明确待办(下一会话顺序)

1. **C03 收尾**(工作树在途):跑门禁(cargo fmt/clippy/test --lib + deep-engine test/typecheck)→ 视状态续写或回退;spec 已冻结支持矩阵,按其推进
2. **A03 native 接线**:telemetry_gpu.rs 同源采样对接 `benchmarkSampleSchema.ts`(cargo 已空闲)
3. **G02 切片 2/3**:命中路由(hitId→setFilter 白名单命令)→ 数据流贯通(filterField 过滤+空结果态)
4. **Deep2D P2-01~04 → P3-01~05**(顺序见权威表)
5. **DE26 第一批余量**:B05(资源预算,依赖 A03/B02)、A04(Web 公平基准,依赖 A02/A03)、E01(双端色彩,依赖 A02/C01)、E02(环境灯光入包)、G01/G03~G05(Dashboard 正式链)、H06/G05(分发与离线交付)
6. **V01 初评**等 A04/B04 接入切片/真机数据齐

## 4. 关键命令

    cd D:/Documents/bim/bim-studio
    pnpm --filter @bim-studio/deep-engine typecheck        # TS 引擎(含 lab)
    pnpm --filter @bim-studio/deep-engine test             # 全量(末尾 sourceSizeGate 7 项历史违规,失败不算新账)
    cargo test --manifest-path packages/deep-engine-native/Cargo.toml --locked --lib
    pnpm --filter @bim-studio/api test                     # 1136 项
    pnpm --filter @bim-studio/web test                     # 3462 项
    node packages/deep-engine/scripts/runtimePurityGate.mjs # 应绿(585+ 源)
    pnpm exec tsx scripts/verify-dashboard-trusted-layout-host.mts   # 可信布局宿主 E2E
    cd apps/web && node scripts/gate-dashboard-c1-input-matrix.mjs d7c63730-393f-4ade-a6ec-60af914d7c0a  # C1/C2 浏览器门禁(服务需 pnpm studio start)
    pnpm exec tsx scripts/generate-de26-evidence-index.mts  # 证据索引(确定性)
    pnpm exec tsx scripts/generate-benchmark-asset-manifests.mts  # 语料清单重生成

## 5. 用户指令与不可改变边界(本会话新增+延续)

- **[已脱敏]目录 RVT 不作语料**;RVT 用 D:/Download 与 test-model(2026-09-17)
- **测试环节不拖长**:聚焦测试优先,全量只在切片合拢时跑一次(2026-09-17)
- 本机 Unity 直接可用不需独占;签名/安装环境在本机(2026-09-17)
- 延续:admin/admin、Postgres+MinIO 拓扑、既有数据、source-available 称谓不变;N0 铁律(任意 JS/DOM 不进 N0);非 Windows/重型光追/UE 虚拟几何等明确排除项继续有效
- 提交纪律:绝不 `git add .`;每切片只暂存自己验证过的文件;**共享文件先 diff 核对外来行**(本会话两次遇到并行改动混入,用选择性暂存解决)

## 6. 踩坑与纪律(本会话新增,勿重复)

1. **bash 内联 node -e 写含反引号/复杂转义的补丁必炸**:一律写临时 .mjs 文件执行(test-output/ 下,不入库)
2. **deep-engine 全量测试链**:`pnpm test` 的退出码会被 `| grep` 吃掉——链式 `&&` 提交前必须单独确认每道门禁真实通过(purity 已绿;sourceSizeGate 仍红是历史账)
3. **Node 直载 .ts 的限制**:参数属性(`constructor(readonly x)`)与 `.js` 后缀导入不被 strip 模式支持;脚本侧用 `pnpm exec tsx`
4. **跨包资产与代码同批提交**(HEAD 自洽):P1-22 前科+本会话 B03 暂缓先例——依赖并行在途文件时先算闭包(`git ls-files` 对比),决定随行提交或明确暂缓
5. **G02 切片 1 教训**:新增编译分支时先确认辅助函数是本文件私有还是需导入(rectangle 在本文件)
6. **子代理派发质量高**:P1-22/P1-23/B03/B04/C02 均一次交付,主线程只需复核门禁+抽 diff+提交;派发词要点=必读清单/硬约束/门禁命令/诚实汇报要求

## 7. Skills

- `engineering-taste`(编码)、`testing-taste`(验收)、`design-taste-digitaltwin`(任何 UI/3D 产出)
- 子代理并发约 2;用户已认可"并行+主线程同时出卡"模式;调研/审查在主线程做
