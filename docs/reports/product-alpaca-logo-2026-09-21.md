# 全局香槟色羊驼 Logo 验证

默认 Web Logo/favicon、PNG 兼容资源、桌面 PNG/ICO/ICNS 已统一；Native 及场景客户端沿用现有品牌消费路径。源 PNG 原样保存，SVG 自包含嵌入原图，未进行矢量描摹。

## 证据

证据目录：`test-output/logo-alpaca-20260921/`（本地产物，不提交）。

- 品牌生成器逐字节复验、public-brand 和 repository 门禁通过。
- API 原生图标/独立 EXE 测试 8/8，桌面场景打包测试 8/8。
- 实际 Native 打包验证产物保留源 EXE 与归档 hash；Windows 资源 API 读回的六档图标与当前 ICO 全部一致。
- 登录与品牌预览实际页面，两轮 × 1920/980/480 × 深浅主题，共 24 张截图；图标加载、比例、视口边界断言通过，pageerror 为零。人工检查各轮代表截图。
- 登录时现有 API 返回 HTTP 502，已登录管理页未复验；未将品牌预览当作真实登录成功证据。
- 桌面完整构建被 `apps/web/src/components/AiResponseEvidence.tsx:71` 的 TS2375 阻断（可选 labels 的 undefined 类型）。未修改该并行组件，未生成新桌面 EXE/NSIS/MSI。

## 视觉检查

对标用户选定稿及 design-taste-digitaltwin 的克制呈现，使用现有 base.css 令牌。以下为 Logo 范围自评，不代表全产品评分。

| 维度 | 自评/范围 |
| --- | --- |
| 布局构图 | 9，原比例保留，无裁切 |
| 令牌一致性 | 9，无 UI 令牌改动 |
| 排版 | 9，图形不带文字，现有标题保留 |
| 交互状态 | 不适用，静态资源 |
| 动效 | 不适用 |
| 3D 渲染 | 不适用，使用预渲染图像 |
| 信息设计 | 9，仅替换既有标志 |
| 反馈即时性 | 不适用 |
| 响应式与主题 | 9，三档尺寸/双主题图标显示通过 |
| 语义与文案 | 9，品牌名称与页面文案不变 |

同族检查覆盖双 SVG、三 PNG、桌面三档 PNG、六档 ICO、ICNS、Native 注入和场景包品牌资源收集。剩余为修复现有构建错误后重新生成桌面安装包，以及服务恢复后的管理页复验。未提交或推送。

尺寸复验：按用户反馈收紧透明留白，主体等比放大约 28%。重新生成全部资源并重跑两轮页面截图和 Native 图标读回。

### 2026-09-22 构建阻断核销

上文 TS2375 为当时构建记录，当前 `AiContextDeliveryEvidence` 的 `labels` 参数已显式接受 `undefined`；本次复跑 `pnpm --filter @bim-studio/web exec tsc --noEmit` 退出 0，无诊断。无需重复修改 AI 组件。桌面安装包重建和安装后品牌检查仍留最终发布验收，类型检查通过不等于已生成或验收安装包。历史 API 502 也不能直接当作当前服务状态，最终管理页检查以届时实际请求为准。
