# README 内容核查（2026-09-24）

## 现状核查

### 已有（不重建）

- 根目录中英文 README、`docs/capabilities.md`、应用内文档中心和部署指南已经存在；本轮只校正文档入口，不新建第二套产品文档。
- `packages/contracts/src/` 已有项目、应用、场景、看板、数据、语义模型、AI、工业格式、机器人、仿真、参数化、Plant Lite、PPR Lite、虚拟调试、通知和发布合同。
- `apps/web/src/appRoute.ts` 与 `AppPlatformRoutes.tsx` 已消费管理中心、2D/3D/拓扑编辑器、资源库、模型优化、参数化、数据、视觉、运营、系统、品牌、预览和发布路由。
- Web/API/Engine 的 `package.json`、Native `Cargo.toml` 已声明当前实现所用依赖；本轮不新增依赖。
- 各子系统已有对应 `*.test.ts(x)`、Rust 测试、`docs/reports/` 与 `test-output/` 证据；当前 README 中列出的功能并非从零建设。
- 已读取恢复总账、当前交接、工业格式权威计划及产品/引擎集成规格。工业格式目录中的计划、合同或探测器不等于正式可导入能力。

### 真实缺口

- README 的“功能亮点”和“完整功能列表”落后于当前产品入口，缺少资源、拓扑、数据治理、脚本、工业仿真、机器人、参数化、运维、插件和多端交付等功能族。
- 当前工业格式段把规划项放进功能列表，容易把目标目录误读为可用能力；应只写已接入或明确标注的受限预览能力。
- README 含路线图/执行计划入口、平台对比或借鉴线索，以及面向普通读者价值有限的内部规格链接。
- 中英文 README 的产品范围、许可描述和入口不一致；本轮同步功能结构与有效链接，但保留用户指定的中文“作者的话”“参与贡献”“许可”“鸣谢”原文。

## 本轮处理

- 用五条核心亮点概括价值，再按产品子系统列全已落地能力。
- 删除 README 中的路线图、执行计划和平台对比入口；不把规划能力写成已完成。
- 检查 README 的本地链接与远程链接，删除失效或对首次阅读无帮助的文档链接。
- 运行 Markdown 链接校验、关键词扫描和仓库治理门禁。

## 交付结果

- 功能亮点已突出 AI 可理解/可执行、自研引擎性能、合同与插件扩展性，以及工业创作和多端交付。
- 完整功能列表已补齐项目资源、格式、3D、2D/拓扑、数据语义、脚本、AI/视觉、工业仿真、引擎、发布、SDK/插件和系统治理。
- 新增技术栈；快速开始覆盖依赖、克隆、配置、初始化、启动访问、素材包、数据库、打包发布、Docker 现状、常用命令和开发注意。
- 路线图、内部执行计划和平台对比入口已从中英文 README 删除；用户指定的中文四个区块保持原内容。
- README 本地链接检查通过；远程依赖/鸣谢链接可访问，CI 工作流文件在仓内存在。

## 三个 CI 徽标补修（用户截图）

### 现状核查

- 全仓检索中英文 README、相关源目录与未跟踪文件后，确认仅页首三个 GitHub Actions 徽标损坏；原 SVG 地址均返回 404，仓库首页对匿名访问也返回 404。仓内三个对应 `.github/workflows/*.yml` 实际存在，故不是文件名拼错。
- `packages/contracts/src/` 不定义文档徽标合同；`package.json` 与 Cargo 依赖无需改动。两份 README 是消费方，仓库治理门禁校验两份入口；已有 `docs/reports/`、`test-output/` 和本报告提供证据。已核对当前交接、规格与恢复总账，未发现另一个徽标资源或部署合同可复用。
- 真实缺口是私有/尚未公开仓库的匿名状态图片不可加载；继续引用动态 Actions SVG 会在公开前一直显示破图。

### 修复与验证

- 仅替换中英文 README 各三条徽标引用：图片改为仓内静态 SVG，点击目标改为仓内对应 workflow 文件。静态图只标识 CI 工作流，不伪装测试通过状态；仓库公开后如需实时状态，可再恢复 Actions 动态徽标。
- 三个 SVG 通过 XML 解析，六个 README 图片/目标路径均存在；分别在深色和浅色背景渲染截图复核，标签完整无裁切。预览证据为 `test-output/readme-badges-preview.png` 与 `test-output/readme-badges-preview-light.png`。
