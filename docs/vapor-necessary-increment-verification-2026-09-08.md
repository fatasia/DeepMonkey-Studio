# 必要渲染增量修补 · 2026-09-08

## 已完成：r47 限定验证

从 Vapor 方案吸收“减少重复工作、按变化更新”的原则，不迁移 React/Three.js，不恢复专项性能任务，不改 GLM 原始草案。

- 灯光辅助虚线原先每帧标脏 position 并重新建立 lineDistance。现在量化到实际 Float32 后比较端点，仅变化时上传；复用距离属性并更新包围球，保留手柄随相机缩放/朝向与虚线语义。
- 预热调度器销毁后不再接受任务；取消回调即使迟到也不执行，在飞任务的成功/失败不再覆盖销毁状态。未声称可取消驱动中已经开始的编译。
- 红测实际出现四个失败：1000 帧重复标脏、距离属性被替换、销毁后成功/失败改变状态。修补后加上重合端点、迟到取消回调和原相机回归，3 文件 14 项通过。
- r47 根 build/typecheck/test 全部退出 0；2159 源文件均 ≤800 行，Web 431 文件 1784 项，API 129 文件 587 项。首屏 JS 314.0 KiB / gzip 103.4 KiB，与上一批同口径未增加；不是整机 FPS 结论。
- r47 Web index SHA256：`e29bfea965b034a6a89ba25c3bb549b5d64d859c58b7a530dc70736afc2a158f`。

真实浏览器 `light-editing-8pPg9o/report.json` 两轮 dark1440/light980 4/4：创建光源、改位置与目标、保存刷新、退出；仅无遮挡3D裁区的像素变化为1740/68/1742/68，错误与警告0。`TyKciC` 初版元素截图包括上方浮层，不作为纯3D像素证据；其表单操作与保存结果仍可追溯。原账号、正常 PostgreSQL+MinIO、原场景均未改。

参考 [Three.js Line 官方文档](https://threejs.org/docs/pages/Line.html) 的累计距离语义；未重新实现渲染器或声称 Vapor 可以直接用于本 React 应用。

## 本轮待办：截图发现的窄窗环境工具问题

主线程亲审两轮图片发现 980px 下环境浮层使无遮挡场景宽度仅58px，右侧检查器仍占位。已进入 r48 修补：环境编辑时临时收纳右栏、关闭恢复原偏好，增加直接关闭按钮，冗余保存/GI说明移到悬浮提示；13项相关聚焦通过，尚待统一构建及两轮新截图。

十维审查：r47布局8（上述窄窗问题）、令牌9、排版9、交互状态9、动效不适用、3D仅灯光辅助线专项9、信息设计8.5（冗余说明）、反馈9、响应式8、语义文案8.5。不能用渲染测试通过冒充整体视觉达标；低分项在r48继续修正。完整场景画质、任意客户规模与全站性能仍按总账推进。

## 明确排除

不修改依赖框架、渲染后端策略、画质档位或用户保存的场景；不恢复 WebGPU 8小时长稳、旧自动任务或暂停的专项性能。仅本地提交，不 push。
