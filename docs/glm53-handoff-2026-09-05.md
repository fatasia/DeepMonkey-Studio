# GLM-5.3 → GPT-6 开发交接（2026-09-05）

分支：`dev-studio`（最新提交见 `git log -1`）。**严禁 push**；admin/admin 与 postgres+minio 拓扑禁改；大改先经用户同意。

## 必读顺序
1. `AGENTS.md`（含多会话协作纪律与深测条款）
2. `docs/active-task-recovery-ledger.md` §15.4（当前队列与进度权威）
3. `docs/zcode-glm53-handoff-2026-09-04.md`（Codex 交接，SIM-0/U1-4 已完成）
4. `docs/specs/`（各规格 §8 回填=已完成）

## 本轮（GLM-5.3）已完成
- S1-001 数据语义层（合同/校验/CRUD，`069fa1d`）
- U1 系列修复：更多菜单点击无效（`6946233`）、画布抖动（`7a12192`+`b6929a1`）、真实缩略图（`c0aada2`）、Node-RED 白屏（`762c491`）、监控面板位置、顶栏去重、面板间距、说明墙删除
- EX-001A：空白拖拽框选（相交命中）、Shift/Ctrl 加选、Alt+拖拽复制、右键选层遮挡栈（`3099c02`/`da187ae`/`6b5929f`）
- 2D 底部页签恢复（`9d8a7c7`，用户反转旧决策，断言已更新）
- 图层右键菜单、SIM-1a 合同/持久化/场景树仿真域/检查器挂载（`046c773` 起四提交）
- 素材：HDRI 55 / PBR 72 / 开放包 16 / Sketchfab 164 GLB（key 在 gitignored `data/external-assets/source-b/api-keys.env`；脚本幂等可重跑，Sketchfab 有每日配额限制）

## GPT-6 接手任务（按优先级）
1. SIM-1a 收尾：实体编辑写回从草稿 ref（AppStudioShellView `simulationEntityDraftRef`）改 controller 直连保存；引擎无关覆盖层（路径线/轨迹）
2. U1-8a 双主题截图补档（功能已修）
3. <30% 缩放 Ctrl+滚轮人工复验（自动化已过）
4. S3-B/S3-C 规格（2D 组件 300+、行业模板 300+）
5. 升级清单：18 个 >500 行文件拆分（DashboardWorkspace 现 789）、浅色主题全量令牌化、hooks 补测试
6. 框选 E2E 脚本稳定性（u112b 偶发假阴性）
7. astral-service 分析（克隆在 `D:\Temp\astral-service-analysis`）

## 验证命令
```powershell
git status --short; git log -5 --oneline
pnpm --filter @bim-studio/web typecheck
pnpm --filter @bim-studio/web test
pnpm quality:source-size
pnpm studio start web --no-open
```
全量基准：Web 317 文件 / 1113 测试全绿；1689 源文件 ≤800 行。

## 补充：素材下载产出瓶颈（2026-09-05 大批量收尾数据）
23 关键词整批统计：366 个结果无直连 GLB（Sketchfab 仅提供 glTF zip）→ **给 `sync-sketchfab-models.mjs` 加 zip 下载+解包可把产出提升约 3 倍**（GPT 接手素材线时优先做此项）；67 个许可不符正确拒绝；catalog 稳定 164。另受 Sketchfab 每日下载配额限制，幂等重跑即可续传。
