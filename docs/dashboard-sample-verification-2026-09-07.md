# 生产模板示例数据：阶段验收

## 范围与状态

最新执行顺序：2 素材模板 → 3 填报导出 → 6 仿真/AI → 1 V3 → 4 UI → 5 Vapor → 7 最终验收。本文只记录第2项的可运行样板增量，不代表第2项全部完成。

### 已完成

- 新增作者示例数据合同：有限标量、100行/16列/128KiB限制，禁止危险字段名，不允许与真实数据产品、直接网关、语义绑定混用。
- 生产运行总览沿用原模板 ID 与9个可编辑节点，附3条手工编写的产线示例。四指标分别为产量求和、OEE算术平均、告警求和、准时交付率算术平均；不是设备实测或加权工厂OEE。
- 数据页可整组改数，不必解组。数字非法可见提示，应用/取消/Esc、增行、共享快照单命令更新；复制节点/页面会隔离快照ID与指标键。
- 示例随普通应用保存/发布，匿名浏览仅在本地计算示例与筛选，不开放受保护数据网关。修复匿名页原先不消费示例以及筛选后变量旧值遮盖空结果的风险。
- 标题与筛选器布局不再重叠。示例编辑器使用既有品牌/语义令牌，截图复检后加固定列宽和横向滚动。

### 验证证据

- r27根构建（含API导入smoke）通过；r29 Web 405文件/1634测试、根typecheck与2066源文件体量检查通过。
- r29浏览器 `test-output/codex-2026-09-05/dashboard-samples-Chsw9p`：两轮×深1440/浅980，4/4通过；包括非法数字/Esc撤回、整组修改7个数据组件、取消新增行、保存→刷新→筛选→CSV→发布→匿名刷新。正常项目场景结构未变；产品控制台错误/警告为0。
- 800/480也保存截图，但480顶栏仍挤压，因此不将截图存在视为全站响应式通过。未测试外部客户数据写回、权限治理、真实设备或专项性能。
- r30最终：Web build、405文件1634测试、Contracts20文件201测试通过；两轮真实浏览器 `dashboard-samples-MkpwSQ` 4/4通过，深/浅编辑与匿名页截图已人工复看。进一步限定表格列宽、主题按钮，图表入场动画结束后截图正常；没有把动画首帧空图误报成渲染故障。Web index SHA256 `4abac29507e8020dcf2d0c1bc52053e7db35cbdbf43ba84526d1c117f0601f70`。根类型/体量归属r29，r30只有局部CSS与表格宽度及截图等待变化，不伪称全仓在r30重测。

### 对标与十维自检

按仓库数字孪生设计技能，采用既有[对标计划](editor-interaction-benchmark-2026-09-04.md)中的FVS可编辑模板/字段工作流与西门子简洁信息分组；本批没有重新采集外部平台现状，不声称全面领先。

| 维度 | 本批判断 |
| --- | --- |
| 布局 | 8：编辑表格列宽已复修；全页模板明细区域留白较多 |
| 令牌 | 9：新增控件复用既有双主题与品牌色 |
| 排版 | 8：实际1920画布缩到小编辑视口时文字偏小，待第4项整体收尾 |
| 交互 | 9：整组改数、取消、错误、保存、筛选、发布链路已测 |
| 动效 | 未新增；图表复用ECharts，完整动效专项未验 |
| 3D | 不适用，本批不调整渲染器 |
| 信息设计 | 8：一份明确标注的生产示例，不是多页行业闭环 |
| 反馈 | 9：应用/错误即时展示，不冒充性能延迟基准 |
| 响应式主题 | 8：980与双主题可用，480全站顶栏遗留按第4项处理 |
| 语义文案 | 9：示例与真实数据分离，数值口径明确 |

未达全项9分，不宣称Kimi-95总验收完成；技能促成了两轮截图检查和列宽/按钮主题修正。

### 本轮待办

第2项：完整5–10页行业业务包、真实可放大缩略图、资产绑定与说明、≥10包/≥300页面；163个模型仍待视觉审查。现有模板库仍是120条，本批只深化其中一条，不额外计数。

示例编辑仍需补列定义编辑、清空后字段类型保持、跨页样本复用和更完整的复制/撤销浏览器矩阵。它是模板作者数据，不是第3项的业务填报写回；填报权限/冲突、分页页眉页脚、Excel/图片导出均未在本批实现。

第1项V3-P2的两个源码改动保留未提交：红测确认`sceneApplicationSync`漏剥离三个发布字段；聚焦测试通过，真实浏览器返回二维复核按后置顺序继续。原报告推测的桌面适配器写入对象是Snapshot，不可照报告删其发布信息。

未改admin/admin、Web模式、PostgreSQL/MinIO拓扑、原场景与原模型；不push。GLM的Vapor/Shader原文件保持不改、不擅自混入本批提交。
