# 主线高收益优化与能力接入表（2026-09-20）

用户本轮要求：将底层优化、高价值能力缺口和六家引擎值得借鉴的设计纳入当前任务。本文是既有任务卡的执行补充，不另建引擎或替换权威路线图；与旧待办冲突时，按最新提交和证据扣除已完成部分。

## 1. 纳入范围与现状

- 恢复入口：[当前交接](../handoffs/mainline-closeout-2026-09-20.md)、[总账](../active-task-recovery-ledger.md)、[权威路线图](de26-master-execution-roadmap-2026-09-19.md)。本表各行状态为“本轮待办”，仅剩余条件满足后关闭；已完成切片列入复用依据。
- 本次核对 HEAD `fe18a12`：R10 F04/F05 为 `8dc7b14`，F06 motor/limits 为 `fe18a12`；R12 生产 PBR 捕获为 `726d8fd`。不重复实现已完成夹具、捕获事务、staging ring、HiZ 尾巴或 RenderGraph 编译计划。
- 当前切换由 `apps/web/src/viewer/StudioDeepWebGpuBridge.ts` 保留 Three 作者状态并切换输出画布；`viewerEngineSimulation.ts` 仍直接初始化 Rapier。兼容场景切换不等于全部能力互换，新确定性验证不等于产品物理宿主已接入。
- 历史收益只作为选题依据：C3 transform-only 5000 实例更新 187ms→15–19ms、HiZ 内核 26–32%、wgpu 约占帧预算 3%。这些口径不同，不能相加或推定当前整帧收益。本次只调整计划，未重新测量。
- 当前工作树存在多会话源码与资产 WIP；本轮只新增本文和恢复入口链接，不修改源码、机器完成状态或既有证据。

## 2. 底层优化执行表

“预期收益”是优化目标，不是承诺数字。每行先检查最新实现；已满足条件只归档证据，不重建。新增文件预期均为“优先修改现有模块；必要的聚焦测试/证据文件按认领切片确定”，不得预建平行系统。

| ID / 优先级 | 原任务归属与复用入口 | 剩余动作 | 预期收益与关闭条件 |
|---|---|---|---|
| OPT-01 / P1 性能，优先 | C3 / B01 / B02；既有 projection/scene update 与变更 revision | 核对纹理、LOD、阴影标志、单几何等变更是否仍触发全量重建；保留已完成 transform/material 快路径 | 只上传变化区间；逐类提供编辑操作前后 CPU、上传字节、分配量和 P95/P99，不以微基准替代整帧 |
| OPT-02 / P1 性能 | R4 / D06；既有 HiZ、紧凑实例缓冲、indirect 消费链 | 核查 GPU 常驻、剔除→压缩→draw 的真实链路与读回；补默认关闭功能的能力探测、配置和安全启用条件 | 降低 CPU 提交与搬运；真实产品场景 draw/culled 指标、遮挡正确性及不支持设备回退均有证据 |
| OPT-03 / P1 性能 | B03 / #18 / B05 / R6-3；`src/webgpu/pbrFrameGraph.ts` 及既有 ring/planHash | 审核跨 Pass uniform 覆写依赖；按帧/Pass 管理生命周期，补安全复用与无用拷贝消除；不重做死 Pass 编译器 | 带宽/同步与峰值显存下降；多帧在途无污染；资源别名须满足生命周期与用途约束 |
| OPT-04 / P1 性能 | R8 / B05；既有资源预热、运行包、size-ledger | PSO/资产缓存身份绑定 shader、设备和格式版本；后台预热、取消与缓存失效；浏览器不假设可导出原生 PSO | 冷/暖启动分别报告首次可交互、长帧与内存；旧缓存失效正确，取消无泄漏 |
| OPT-05 / P1 性能 | D05 / G3 / R9；现有 optimizer、runtimePackage、流送/HLOD 卡 | 先补空间分块、LOD/HLOD、压缩与按需流送真实消费，再推进有预算的虚拟几何原型；复用既有派生资产 | 首次打开和峰值内存改善；对象 ID、拾取、属性、剖切不因聚合丢失；禁止扩成完整 Nanite |
| OPT-06 / P1 性能 | R2 / R6-3；`src/shaderCompute/`、bounded scheduler、真实 encoder | 先落地一个非 HiZ 的 buffer 消费点；测热点后调布局/工作组、必要时融合；真正独立任务才并行 | 正确性对拍及端到端净收益；Promise 并发不计作 CPU 并行；线程/Worker 搬运成本计入 |
| OPT-07 / P1 性能，补充验收项 | R6-2 / R8 / H03；`viewerFramePresentation.ts` 和既有播放时钟 | 审计静态场景持续更新，变化驱动或收敛后降频；统一动画、物理、告警、视频、TAA 唤醒条件 | 静止 CPU/GPU 占用下降，唤醒及时；播放、输入、告警不冻结；记录交互长帧而非只平均 FPS |

## 3. 高价值能力与编辑器接入表

下列入口均为现有复用点；预计只在其调用链补适配、入口与定向测试，不创建第二套场景、物理、调试或业务系统。实现认领时需补具体文件边界。

| ID / 优先级 | 归属 / 现有实现 | 缺口与本轮交付条件 |
|---|---|---|
| CAP-01 / P0 | R10 / R3；`physics-validate/`、`viewerEngineSimulation.ts` | 沿 F04/F05/F06 推进剩余关节控制和产品宿主；固定步长、控制更新、保存恢复与重放进入真实编辑/运行链；旧 Rapier 路径明确保留或迁移，不并行维护无主状态 |
| CAP-02 / P0 | R12 / H02 / H03；`src/r12/`、`pbrFrameCapture.ts`、Studio bridge | 生产捕获已完成；补内置 PBR source-map、资源快照和编辑器调试入口。真实帧→Pass→资源→源码可追溯；未映射 Pass 明示，关闭捕获保持低开销 |
| CAP-03 / P0 接入合同，随切片验收 | R3 / H02；`StudioDeepWebGpuBridge.ts`、`useAppRuntimeEffects.ts` | 建同一份后端能力矩阵和切换预检；覆盖高频材质/几何/效果。切换保持相机、选择、编辑状态及撤销上下文；不支持项有具体原因，失败留原画布；XR/离屏等范围外路径不得宣称已支持 |
| CAP-04 / P1 能力 | R11 / F01；既有 AnimationStateMachine、时间线、SceneTransformGraph | 状态机/路径/约束写回产品图，接编辑、保存、播放和运行包；取消/停止恢复可预测；不是另写一套动画核心 |
| CAP-05 / P1 能力 | P5 / P7 / D01；既有 spatial/QTO 计算与导出函数 | 补选择范围→规则/单位→计算→结果定位→导出入口、限高与空/错误态；同场景保存重开语义一致，量值可追溯 |
| CAP-06 / P1 效果 | R1 / G7 / E04 / E05 / E02 / C04-C05；既有 GI、雾、SSR、灯光 Pass | 先接生产链和质量档，再补 SSR 时间稳定性、GI 更新、多灯/阴影与 IES native；预算明确、设备降级可解释，编辑器参数可保存，不以 feature 存在代替产品可用 |
| CAP-07 / P1 能力 | P2/P3/P4 / G06/G08；previewMqtt、DataEventBus、运营页 | 补持续数据→告警→定位→回放的实际数据链和重连/权限错误态；不重建摄取总线和已完成运营 UI |
| CAP-08 / P1 能力 | R5 / H09 / R8；既有编译/事务/发布链 | 学增量构建与工具接口：复用命令/合同提供可校验调用，变化范围驱动编译，失败不污染工程；不新增平行 AI 编译器或无约束写接口 |

每个能力的完成记录必须包含：内核入口、产品调用、用户入口或自动启用条件、保存/恢复、实际运行证据、后端支持边界。只有内核单测不得关闭产品接入卡。

## 4. 六家引擎精华的引入映射

官方资料核查日期 2026-09-20。学习设计不等于复制实现或自动升级依赖；如引入代码/依赖，执行版本固定、许可证和分发审计。没有证实的下一版内容不设交付依赖。

| 来源与已确认阶段 | 值得引入 | 映射任务 / 不引入的范围 |
|---|---|---|
| [Unity 7 路线](https://investors.unity.com/news/news-details/2026/Unity-7-Roadmap-Revealed-At-Unite-Seoul/default.aspx)：计划 2026-12 早期 Beta、2027 Q1 正式版 | 变化驱动重载/构建、CLI/public API、可扩展 Surface Cache GI 思路 | OPT-01/04、CAP-06/08；不迁移 CoreCLR，不把未来发布当现成功能 |
| [Three.js r186](https://github.com/mrdoob/three.js/releases)：已发布 | 绑定缓存生命周期、PMREM 与材质散射改进，组合式兼容适配 | OPT-03/04、CAP-03/06；先核对本地版本与 `three-aces-r185` 画面合同，不整包盲升或全插件兼容 |
| [Babylon.js 9.0](https://babylonjs.com/)：已发布 | Frame Graph 资源复用、图形诊断与材质能力一起交付 | OPT-03、CAP-02/06；不启动 Babylon runner，不恢复 A01-X |
| [Bevy 0.19](https://bevy.org/news/bevy-0-19/)：已发布，进一步并行编码仍属后续方向 | ECS schedules 的访问依赖组织、批处理与 CPU/GPU 数据分工 | OPT-02/06、R9；保留现有 RenderGraph，不整体换 ECS 或把计划并行当成已并行 |
| [Godot 4.7](https://godotengine.org/releases/4.7/)：已发布 | 每 Pass 环境 uniform 内存池减少复制依赖、clearcoat 能量正确性；借鉴资源/属性/运行一致的工作流 | OPT-03、CAP-03/04/06；不另造通用游戏编辑器，不承诺未公布的下一版能力 |
| [UE 5.8](https://www.unrealengine.com/news/unreal-engine-5-8-is-now-available)：已发布，官方转向 UE6 | Lumen Lite 的低成本 GI 分档、MegaLights 的多灯预算思路、静态几何流式加载 | OPT-05、CAP-06；不恢复完整 RT、完整 Nanite、影视工具链或跨平台全覆盖 |

## 5. 执行顺序、并行边界与验证

用户最新排序覆盖上表优先级中的调度含义：接入、消费、编辑器挂载和无缝切换统一放到任务末尾的集成阶段；上表 P0/P1 保留问题重要度，不表示提前开工。当前已经在做的切片先收尾，不回退已完成接线。底层优化必要的最小调用/验证不扩展成提前做完整产品集成。

| 批次 | 执行动作 | 依赖 / 并行规则 |
|---|---|---|
| 0 | 当前 R10/R12 在手切片先收尾 | 不撤销或重开已完成提交；FINAL-GATE 第一轮保留，不因新增项无限延迟 |
| 1 | OPT-01/03 等高收益底层优化；R10 剩余核心能力与效果内核 | 认领前检查 diff；同一 renderer/合同文件不得由多车道同时写 |
| 2 | OPT-02/04/05/06/07 的剩余底层部分，GI/多灯等高价值能力与设计借鉴 | 独立切片可并行，GPU 性能采样串行；涉及完整消费链/产品接线的部分留到下一批 |
| 3，功能任务末尾 | CAP-01–08 的接入、消费、UI、保存恢复、后端能力矩阵与无缝切换；OPT 项遗留生产消费统一收口 | 底层能力就绪后集中集成，不遗漏、不取消；复用当前已有接线 |
| 4 | FINAL-GATE 最终产品、性能、效果与回归验收 | 集成后最终签核；第一轮通过不等于全部完成，项目级后验收按原范围 |

- 开工前每项记录现有入口、最新证据、未满足条件、拟改/新增文件，尤其先扣除其它会话的新提交。本表不把“待核查”伪装为“完全未实现”。
- 每个切片保留必要类型检查、受影响路径聚焦测试，以及与其风险对应的最小真实 GPU/浏览器/双端证据。重复全量测试、广域视觉/压力矩阵后置到合并与 FINAL-GATE，不取消质量验证。
- 性能使用相同资产、镜头、设备、画质和版本，记录 CPU/GPU 分段、P95/P99、上传量、内存、冷暖启动；新优化采用预先声明的改善目标和回退判据，不虚构倍率。
- 明确排除不变：wgpu/vendor fork、完整光追/虚拟几何、全生态兼容、未批准平台/工业深水项、A01-X/跨引擎实测、480min soak。竞品资料研究不恢复这些任务。
