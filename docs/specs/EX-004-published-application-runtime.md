# EX-004 发布应用运行链

状态：已完成（公开快照与隔离生命周期范围）；2026-09-05 Codex 接续用户批准的编辑器与全局修复任务。受保护数据发布授权仍为本轮待办，不混称任意数据源可匿名运行。

## 已核实缺口

预览控制台复制 `/apps/:applicationId`，入口却无此页面；后端已有不可变应用发布记录、当前版本指针及历史读取。自动生命周期已在独立预览会话实现，不重写运行器。

## 实施边界

- 独立懒加载只读入口，不初始化作者 store、登录门禁或保存/发布动作；页签、刷新、空页、撤回/404、网络失败重试可用。
- 公开浏览资源只包含该快照引用的模型/素材骨架，不暴露整个项目的数据源、AI及运营配置。
- 脚本和依赖来自发布快照；依赖接口按发布ID与依赖ID精确授权，不可读取其它项目、未发布依赖。仍被草稿或发布引用的依赖不可删除。
- 复用ApplicationPlaybackSession；页面切换与离开释放Worker，运行改动不写作者文档。匿名运行不访问受保护数据目录、直接绑定、AI网关；配置含这些连接时明确说明，不伪造实时数据，也不放开原鉴权。
- 修复复制发布链接的权限失败反馈；只读页不显示返回编辑/发布更新动作。

## 验证

API资源过滤、当前/历史身份、撤回、依赖越界/缺失/保留测试；隔离生产浏览器深浅×1440/980：真实保存→发布→匿名自动加载/交互→修改草稿不影响发布→刷新→新发布→撤回，503重试与缺失入口；API/Worker/控制台记录和真实截图。Web/API全量、类型和构建复查；不代替后续受保护数据发布策略与全项目后验收。

## 回填（2026-09-05）

- 新 `/apps/:id` 懒加载入口与只读资源包，复用 DashboardPlayback，不初始化作者 store/登录门禁。正式版本、详情、重载、参数与页切换、可用错误恢复；交互文本组件支持 Enter/空格，不暴露作者返回/发布操作。
- API按当前指针浏览；历史依赖按应用/版本/依赖精确验证。草稿或任一历史发布引用的依赖返回结构化409并保留，UI区分移除当前引用和保留历史文件。既有历史快照在撤回后的读取语义未擅改。
- 公开页不启用后台目录、直接绑定/SSE、AI、受保护监控解析；相关组件显示未开放，Worker请求给明确错误。公开地图/媒体/Unity静态资源沿原无凭据HTTP(S)读取。旧主线程事件脚本只在作者信任域执行，公开页不执行，详情和日志说明。
- 运行外框改用主题令牌与12px控制文字；画布/3D内容沿发布快照，不用平台主题覆盖作者设计。截图复核另修不可用提示挤压与过重画布阴影。
- `test-output/codex-2026-09-05/published-application-rbGKnK/report.json`：4组合全通过，实际编辑器发布→匿名依赖42/点击/筛选/键盘页跳转/3D对象自动挂载→草稿隔离→新发布→503重试→撤回/无效链接。每组8/8 Worker关闭，零匿名写入、零受保护接口请求。故障HTTP诊断和已知ANGLE X4122 warning单列，不称控制台绝对零警告。初次等待日志的exact文本选择器误判，原截图已证明脚本正常，修夹具不改产品运行器。
- 相邻自动预览 `script-playback-Ma1PqT` 与作者编辑器 `script-editor-gWMgEC` 各4组合通过。全仓测试通过（Web1203/API452及其余包），追加只读/键盘聚焦2项通过；最终Web复跑见交接§18。根类型/生产构建通过，首屏306.7KiB/gzip99.7KiB、11chunks。服务保持健康Web模式，原API已加载新路由。
- 最终权威目录 `published-application-gbSjj2` 四组合通过，显式拒绝剪贴板权限并检查可手动复制发布URL；最终Web341文件/1205项、1811源文件尺寸通过。前三次通过目录保留为迭代证据，不覆盖失败产物。
- 对比度追加门禁先复现亮色选中项2.16:1，再修正为主题派生强调深色。更新权威证据 `published-application-fVTC0t` 四组合全部通过，所测外框文字均≥4.5:1，Web1205复跑通过。不得沿用上一目录来宣称本项之前已达标。
