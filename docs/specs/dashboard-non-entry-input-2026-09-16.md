# Dashboard 非入口页输入修复

2026-09-16。C1 的页面请求层修复，完整 C1 验收继续待办。

## 缺陷与修复

`dashboardCompositionProbe.tick` 携带当前 `expectedSource`，但不重复传 `pageId`。请求解析原先始终把省略的页号解析为入口页；切到其它页后，合法 Tick 和图表更新因此被误判为来源不匹配。

请求解析现按显式页号、来源页号、入口页的顺序选择目标。包 hash、页号、generation、deviceEpoch 的完整来源检查保留。未绑定来源的新发布仍进入入口页，显式跨页更新仍拒绝。

## 验证

- 新增四项请求/候选集成回归：非入口页双图表分别更新与 Tick，切出再返回后的旧事件零分配拒绝，present 失败保持旧页与游标并可重试，显式跨页拒绝与新发布入口语义。
- 原逻辑运行新增回归：2 失败、2 通过；修复后 Runtime Package 18 文件、244 项通过，核心 `tsc --noEmit` 通过。
- 使用既有 producer fixture，候选宿主为受控测试宿主；这些证据仅证明请求归属与事务行为。本轮无渲染/UI 修改，没有新增真实浏览器 GPU 或视觉验收证据。

完整浏览器输入、命中归属、双图表 GPU 交互、设备恢复等仍按 [C1 原验收](codex-dashboard-delivery-slices-2026-09-16.md) 执行。
