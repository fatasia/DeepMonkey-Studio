# 发布对话框视觉与键盘回归

2026-09-15 晚间，真实工作台“D06 冻结版本打包验证 2240”。本次沿用西门子式克制分组、ThingJS 式状态反馈和 `styles/base.css` 令牌；品牌强调色保持用户原有设置。

## 实测

第一轮检查深色、浅色 980×768；第二轮检查浅色、深色 1280×800，补浅色 980 长错误状态。原生窗口 GPU 测试与页面截图分开，不作为同一视觉证明。

- 发布记录刷新后仍为 v1 已生成；未再次创建发布版本。
- 未配置云渲染选中后显示原因，发布按钮禁用。
- Native 提交后显示具体未支持字段和缺少运行证据，弹窗保留；长错误区可滚动，底部按钮可见。
- 修复打开后焦点留在背景的问题。实际验证初始 WebGL 焦点、Shift+Tab 到发布按钮、Tab 回首项、Escape 关闭并恢复原触发器。
- 控制台 error/warn 为空。主题恢复深色，临时视口覆盖已清除。

23:36–23:42 增补两轮错误报告检查：深/浅色 980×768 摘要，浅/深色 1280×800 展开列表与滚动到底。默认场景真实服务端检查返回 9 项问题，历史 Three WebView v1 保留。报告改为简短摘要与原生 details 全量列表，字段名本地化；Enter 展开/收起后 Tab 可进入历史下载按钮。第二轮发现旧长通知遮住操作区，改为“发布检查未通过”，再次截图确认 980 窄窗取消/发布均可见。主题与视口已恢复，最后读取控制台 error/warn 为空。

## 十维检查

| 维度 | 本次状态 | 依据 |
|---|---|---|
| 布局构图 | 9/10 | 双尺寸无按钮遮挡，长内容独立滚动 |
| 令牌一致性 | 9/10 | 双主题保持层级，沿用既有品牌设置 |
| 排版 | 9/10 | 标题及操作单行，内容可读 |
| 交互状态 | 9/10 | 选择、禁用、执行、错误、历史成功及键盘路径均实测 |
| 动效质量 | 未评分 | 本轮未测完整动效与 reduced-motion 实际表现 |
| 3D 渲染质量 | 不适用本弹窗 | D01 场景视觉仍由单独回归负责 |
| 信息设计 | 9/10 | 摘要与全量问题按需展开，长列表独立滚动 |
| 反馈即时性 | 9/10 | 操作后立即见禁用/状态；未独立测量毫秒阈值 |
| 响应式与主题 | 9/10 | 980/1280 双主题；手机断点不在本次结论内 |
| 语义与文案 | 9/10 | 对象名称/字段中文名优先，ID 与路径保留排错；通知不重复长报告 |

焦点、错误摘要、发布弹窗及动作针对性 65 项通过。23:29 Web 全量 3271 项通过、2 跳过；后续精度与通知修改的全量以最新总账为准。

Kimi-95 完整视觉门尚未关闭：完整动效和三维场景验收继续保留待办。截图在本任务浏览器工具结果中逐轮查看，未把登录截图或会话日志提交到仓库。
