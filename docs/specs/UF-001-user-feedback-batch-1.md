# UF-001 用户反馈问题批次一（2026-09-04，9 项）

> 用户原话反馈，按 AGENTS.md"已反馈问题必须查根因、补验证、一次修好"执行。执行者：夜间 GLM-5.3-Flash（直接修项）+ 白天模型（调查/设计项）。每项完成后在本文件勾选并简记证据。

## 分级说明

- **[修]** 当夜可修：根因清晰或范围孤立，修完必须有聚焦测试 + 浏览器截图。
- **[查]** 当夜调查：复现 + 根因定位 + 修复尝试；查不清则写明调查结论转白天。
- **[设]** 设计规格：牵涉信息架构/跨模块，白天出规格再实现，当夜只确认现状。

## 问题清单

### U1-1 [设] 仿真功能并入 3D 编辑器插件面板 ✅ 设计定稿（2026-09-04）

已完成 `docs/specs/SIM-001-simulation-in-editor-design.md`：交互层取 Visual Components 快速通道（拖预制体→点连→播放），能力层全量同级（Plant 队列/班次/故障/随机实验/置信区间、PS 碰撞矩阵/轨迹/节拍、PD PPR、What-if 全保留），单工作台结构参照西门子。分期 SIM-1a~1d；SIM-1a 详细实现规格待 EX/S1 队列消化后编写。

### U1-2 [修] 动画时间线帧图标过大 ✅ 2026-09-04 已修

`.timeline-marker`（styles/scene-workspace.css 454 行）：11px 方块 + 2px 边框 + 2px 外圈阴影的 45° 菱形 → **7px + 1px 边框 + 1px 阴影**，并加 `::after { inset:-8px }` 扩大命中区保证 ≥28px 点击目标（守住门禁）。截图待夜间批次统一补（双主题）。

### U1-3 [查] 多页面"更多"点击无效

用户报告"很多页面的'更多'点击无效"。调查步骤：全局 grep `更多`（含 `More`/`MoreHorizontal` 图标按钮）列出全部入口；逐页在 dev server 复现（场景卡、资源卡、任务卡、面板头部等）；常见根因排查：弹层被 `overflow` 裁切、`pointer-events`、`stopPropagation` 吞掉、`useEffect` 依赖导致菜单立即关闭、`z-index` 低于画布 WebGL 层。修复后按入口逐个回归并截图；**把"更多"菜单可打开性加入 dashboard/manager 视觉验收清单**（防复发回归）。

### U1-4 [查] 模型优化页能力回退，需恢复完整定位 ✅ 2026-09-04 Codex 会话已恢复（5dc1e38）

交接文件 `docs/codex-glm53-handoff-2026-09-04.md` §3：优化器恢复四阶段管线（导入→转换→压缩与优化→项目素材），保留全部既有能力（减面/Draco/贴图压缩/烘焙等），支持全平台格式经转换服务进管线、素材库双向链路。Web 全量 316 文件 1,111 项测试通过。**夜间不要重做**；仅在使用中发现缺口时按 [修] 流程处理。

### U1-5 [设] 资源页卡片"导入"按钮语义错误 + 缩略图不对

用户：资源页每卡导入按钮没有意义——应该从场景编辑器内导入使用；资源页应支持**浏览**。且二维资源、工业预制体、看板模板缩略图"不大对"。
设计规格 RES-001（白天写）：资源页 = 发现/浏览/预览/详情（收藏、最近、分类、来源、许可），卡片主按钮为"预览/详情"，删除卡片"导入"；插入动作只在编辑器资源面板出现。缩略图三处排查：DashboardComponentCatalog（2D 预览）、industrialPrefab 缩略图、DashboardTemplatePreview（模板缩略图）——先核对当前渲染与目录数据是否错位/拉伸/占位图，[修] 级修复缩略图本身，[设] 级改卡片交互。

### U1-7 [设] 模型优化与资源页逻辑整合

两者都是"资产入口"：优化器产出（减面/Draco/烘焙 GLB）应可直接进资源库被场景使用；资源库大模型应可一键送优化。合并进 RES-001 设计（统一"模型资产"动线：上传→优化→入库→场景使用）。

### U1-8 [修+查] 数据中心三处

a) **[修]** 连接运行监控区域显示位置不对（diagnostics 面板位置/排版），对照 DataCenter.tsx 接入数据 Tab 布局调整到合理位置（连接列表同级或下方，随滚动可达）。
b) **[设]** 处理逻辑（管道）与发布接口两页功能不完善且复杂——并入 EX-007 数据中台向导化规格（已排队），白天补"管道线性主路径 + 高级图模式双层"与"接口发布一步到底"设计。
c) **[查]** 高级接入（Node-RED）页面白屏：dev server 打开复现；查 NodeRedStudio 组件（iframe 加载失败时的降级态、`/api/node-red/health` offline 时的展示、iframe height/加载错误处理）；修复为失败有明确状态不白屏。

### U1-9 [修+查+设] 三维编辑器与发布页多项

a) **[查]** 3D 编辑器窗口缩小后，滚动会**反复跳**：复现（缩放窗口 + 滚动），疑点：视口 resize 时 `fitCanvasToViewport`/相机重置/滚动容器焦点抢占；修复后做 1280→960 缩放往返回归。
b) **[修]** 发布功能页面排版完全错误 ✅ 2026-09-04 已修（代码层）：`ScenePublicationDialog.tsx` + `scene-manager.css`——删英文眉题 PUBLISH CENTER（沿既有决策）；三组选项卡 122/96px 高块（双行长描述）改 44px 单行紧凑卡，描述全部转 `title` 悬浮（用户要求）；弹窗 900→640px、头部收紧；徽章改流内第三列不再绝对定位。测试 1/1 过；**双主题截图待夜间批次补**（浏览器证据未做，不算最终关闭）。
c) **[设]** 云渲染无法一键开启；全局设置应默认配好、可一键开启：CloudRenderControl.tsx（660 行）现状调查 + "全局设置默认值 + 一键开关"规格（白天 CLOUD-001）。
d) **[修]** 场景卡片浏览缩略图不对：应**默认截取场景最后一次保存/发布的画面**。定位场景缩略图生成链路（保存时视口截图？）；修复为保存/发布时自动截取当前视口为卡片图；无截图时回退现有占位。
e) **[设]** docs 文档页"不全、太简单"：DocsCenter 15 篇按使用主链补齐（新建→导入→编辑→绑定→发布→运维），并入 S3-C 内容线规格。
f) **[修]** 场景卡片"更多"里的几项放回主卡片用图标展示 ✅ 2026-09-04 已修（代码层）：SceneManagerView.tsx 主卡动作区新增 图标直达——版本历史（已发布时）/复制场景/重命名场景，"更多"菜单移除重复项仅留真正低频（查看发布版/复制发布链接/撤回发布/云渲染/导出）。测试 2/2 过；**浏览器截图待夜间补**。本次反馈覆盖 821ca74"高频动作精简"的旧决策，交接文档已记录。

## 执行顺序（夜间）

U1-2 → U1-3 → U1-9b → U1-9d → U1-9f → U1-8a → U1-8c → U1-4（查）→ U1-9a（查）→ 其余 [设] 项写调查结论转白天。

## 完成回填

- [ ] U1-2 已修 + 截图
- [ ] U1-3 根因 + 受影响入口清单 + 修复 + 回归
- [ ] U1-4 调查结论 + 恢复项清单（或转白天）
- [ ] U1-5 缩略图 [修] 部分
- [ ] U1-8a/c 修复 + 截图
- [ ] U1-9b/d/f 修复 + 截图
- [ ] U1-9a 调查结论
- [ ] [设] 项转白天规格清单：SIM-001、RES-001（含 U1-7）、CLOUD-001、EX-007 补充、S3-C 文档线
