# 香槟色羊驼 Logo

用户选定香槟金、奶油白、石墨灰及冰青细节的羊驼图形，替换全局默认 Logo，覆盖此前蛇形标志。品牌名称与客户自定义品牌不变。

## 开工核对

已检查恢复总账、2026-09-20 交接、蛇形 Logo 规格、近期提交、工作树及现有生成器。复用 DEFAULT_PRODUCT_BRANDING、Tauri/NSIS/WiX 图标配置及 Native 打包默认图标注入；不重建品牌服务。

| 状态 | 现有入口/证据 | 本轮处理 |
| --- | --- | --- |
| 已完成 | Web 双 SVG 入口、桌面 icons、Native 图标注入 | 保留消费路径 |
| 本轮待办 | 原生成器以蛇形 SVG 为源 | 改为选定 PNG 母版，等比生成全部派生资源 |
| 明确排除 | 客户品牌、历史发布包、已安装应用 | 不覆盖或追溯修改 |
| 项目级后验收 | 整体产品验收 | 本轮专项不能替代 |

新增文件为本规格、报告、`apps/web/public/brand/logo-source.png`。PNG 母版逐字节保留用户选定原图；SVG 采用自包含 PNG 嵌入，不是纯矢量。各平台 PNG/ICO/ICNS 使用 contain 等比缩放，透明补边，保留原配色和质感。通过 `node scripts/generate-product-brand.mjs` 重建，`--check` 校验全部派生文件。

母版 SHA-256：`7b33fac2c6082a0403d0561051a4d22bffed7a0174aa360b48127490dde875ff`。

设计对标为用户选定稿；产品内呈现沿用 design-taste-digitaltwin 的西门子式克制与 base.css 令牌，不引入新 UI 颜色。验收见 [报告](../reports/product-alpaca-logo-2026-09-21.md)。

用户反馈主体偏小后，展示窗口收紧至母版坐标 (139,121)，980×980；母版仍逐字节保留。主体高度占比由约 73% 提升至 94%，相同显示尺寸下约放大 28%，保留安全边距；SVG 与所有平台图标使用相同窗口。
