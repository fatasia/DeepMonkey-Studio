# Deep Engine D01 回归与发布预检前置

日期：2026-09-15。当前窗口负责 App/hooks、发布动作与预检；Deep2D、运行包及导出适配由另一窗口负责。范围入口：[后续开发任务](deep-engine-next-development-tasks-2026-09-15.md)。

## 已完成

- App 模型等待与应用交互逻辑抽离后，新增模型加载 9 项、交互事件 14 项、生命周期 15 项测试。覆盖失败/超时、双总线卸载、重挂载、自动保存取消、发布渲染决策和会话恢复。
- 修复会话失效后旧 `api.me` 响应重新设置用户的竞争；失效事件同时取消启动恢复和重试计时器。两项反例先失败，再转绿。
- 修复 Deep 异步同步完成后重放旧相机、选择与视口的问题。已有新作者帧时跳过旧视图补绘，由既有追帧流程接续；保留引擎辅助层版本单调检查。选择清除、相机和尺寸变化三项反例覆盖同一根因。
- 发布检查新增 `assessScenePublication(project, scene, referenceScenes)`，只审计指定快照，用其他场景解析跳转。新增模型/基础体共享 ID 和 widget ID 重复阻断，并使基础体交互目标正常通过；原项目级入口继续使用。

## 验证证据

以下命令在本地工作树执行，未提交状态包含并行窗口改动。

| 检查 | 实跑结果 |
|---|---|
| Web 模型、交互、场景同步、事件宿主 focused | 4 文件 / 27 项通过 |
| Web `useAppLifecycleEffects.test.ts` | 15/15 通过；会话失效两项有红→绿证据 |
| Web bridge 与 `studioDeepEditorOverlay.test.ts` | 35/35 通过；版本倒退反例有红→绿证据 |
| Web `publicationReadiness.test.ts` | 16/16 通过 |
| `pnpm --filter @bim-studio/web test` | 535 文件通过 / 2 跳过；2752 项通过 / 2 跳过，退出 0 |
| `pnpm --filter @bim-studio/web typecheck` | 退出 0；另一窗口的相机 fixture 缺 `mode` 已由其修复 |
| `pnpm quality:source-size` | 4069 个源文件通过，无豁免 |
| `pnpm gate:repository` | 治理测试 5/5、仓库检查通过 |
| `pnpm --filter @bim-studio/web build` | 最终修复后退出 0；首屏 339.5 KiB / gzip 111.7 KiB，预算通过 |

`App.tsx` 为 795 行、`useAppRuntimeEffects.ts` 为 769 行。构建仍有既有外置模块和大 chunk 提示。

### 真实浏览器

本地 Studio/API 通过 `pnpm studio start web --no-open --core-only` 启动并健康，使用现有存储配置。Codex 浏览器访问真实场景“9999”，显卡检测为 NVIDIA Lovelace。

1. 初次进入时保留已有恢复副本，选择“稍后处理”。进入渲染设置、启用 Deep、Esc 关闭成功。
2. 修复前，选择立方体、清除选择和改变视口后，诊断明确显示 `Editor overlay revision went backwards.`，回退 WebGL。
3. 修复后重新加载，重复两轮选择/清除与 980×768、1440×900 视口变化。两轮截图保持画布和控件可见；最终渲染诊断仍为 Deep，控制台 warning/error 均为 0；1440 视口文档宽度为 1440，无横向溢出。
4. 测试结束恢复 WebGL 偏好和默认浏览器视口。未点击发布；首次通过“返回场景管理”退出时产品自动执行保存，场景更新时间变为本轮时间。后续重挂载使用页面刷新。

截图在本任务的浏览器工具结果中逐轮查看，未将原始会话输出提交到仓库。此次性能面板混有启动、交互和并行测试负载，不用作引擎性能对标。

### 视觉回归自评

沿用工作区 `design-taste-digitaltwin` 的十维检查口径；对标意图为 ThingJS 的选择联动、Unity 的帧状态一致性和西门子的克制界面。视觉令牌来源仍为 `apps/web/src/styles/base.css`，本批没有新增样式或令牌。

| 维度 | 本次评分 / 状态 | 依据与待补 |
|---|---|---|
| 布局构图 | 8/10 | 980 下工具条换行，作者已有构图靠近工具条；未做构图调整 |
| 令牌一致性 | 9/10 | 无样式变更，沿用品牌强调色 |
| 排版 | 9/10 | 对象行、属性数值与控件在实测尺寸可辨识 |
| 交互状态 | 9/10 | 选择、清除、禁用、初始化、失败回退与成功状态已观察 |
| 动效质量 | 未评分 | 本批未验完整相机/动效体系 |
| 3D 渲染质量 | 未评分 | 固定基础体场景不能代替真实 BIM 画质验收 |
| 信息设计 | 9/10 | 目录、视口、属性三层信息保持一致 |
| 反馈即时性 | 9/10 | 实际选择、清除与切换均有状态反馈，未单独测量 100ms 阈值 |
| 响应式与主题 | 8/10 | 已测 1440/980 深色；浅色和其余断点待验证 |
| 语义与文案 | 9/10 | 已验证状态可定位失败原因，术语保持原有约定 |

Kimi-95 完整视觉门仍为本轮待办，不由这次局部修复关闭。

## 本轮待办

- D01 完整浅色/相关断点与视觉收口，以及可独立提交的文件归属审查。
- D02 公共兼容报告合同已实现，完整 Native 能力检查与降级确认仍待接入。
- D03 已接保存后引用检查与确定快照原子发布，详见 [快照一致性记录](scene-publication-snapshot-consistency-2026-09-15.md)；云会话版本守卫、迟到结果与完整预检仍待完成。
- D05 打包失败关闭对话框已在后续切片修复（控制器 7 项测试通过）；发布版本与产物持久化状态、原版本重试仍待实现。

## 并行边界

另一窗口独占 `runtimePackage/**`、`webgpu/runtimePackagePrewarmAdapter*`、`delivery/sceneSnapshotRenderPacket*`、`delivery/sceneClientPackage*` 及 Deep2D 专属实现。共享导出签名或跨端合同变更先协调；本窗口复用其完成的转换结果。
