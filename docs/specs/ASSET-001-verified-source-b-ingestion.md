# ASSET-001 可追溯模型素材补充

状态：本轮待办（下载、真实渲染、2项统一入库已完成；优化另存/交付追溯继续）。用户已重新授权补充缺少的模型/模板，但必须是真实内容与正确缩略图。source-b原有164项历史缓存，不计为本轮新增。

## 已核实问题

- `sync-sketchfab-models.mjs`忽略partialPath，直接写最终GLB；只看服务声明大小，不限制实际流量；无GLB结构验证；缩略图失败仍写引用；搜索异常/坏目录被当空；新增统计恒真。`git ls-files --error-unmatch`确认该脚本已有版本跟踪；之前组合命令的单行输出被误读为gitignore结果，已纠正。密钥与缓存不得加入提交。
- 统一素材目录读取source-a与环境资源，尚未消费source-b。不能只把文件下载到缓存就称已补进素材库。
- source-a专用审计带其独立策略/ID/清单格式，不能借用来发布source-b。旧source-b许可字段缺少版本和完整来源链接，未经补核不得自动发布。

## 实施

1. 有字节上限的同目录临时下载→格式与哈希校验→原子替换；失败保留原文件、清除本次临时文件。坏目录报错，目录写锁防并行丢记录，新增/复用/失败如实统计。
2. 仅接纳明确CC0-1.0或CC-BY-4.0且作者/原始模型页/许可链接齐备的记录；不从“免费”推导再分发权。保留来源和署名，不因中性产品品牌要求删除必要许可信息。
3. GLB校验与真实浏览器渲染；缩略图对应当前模型和哈希，不以来源宣传图冒充本地渲染结果。不合格保持review-required。
4. 验证通过后接统一素材库，许可证/署名随导入和交付保留；导入→优化→新资源→场景复用既有合同与转换队列，不改原文件或存储拓扑。

## 验证

截断/超限/断网/错误GLB/假图片/并发锁/目录损坏/许可拒绝/准确计数聚焦反例；真实下载只走已授权接口并控制小批量。隔离Web/API验证目录→预览→导入→运行、真实截图和对应哈希，最终回填准确新增数量和许可，不把下载成功当成入库完成。

官方依据：[Sketchfab Download API](https://sketchfab.com/developers/download-api/downloading-models)、[API使用条件](https://sketchfab.com/developers/terms)、[CC BY 4.0许可与署名要求](https://creativecommons.org/licenses/by/4.0/)。2026-09-05核对；站点正文部分403，下载端点与格式由官方搜索索引补核，逐模型以接口实际许可元数据为准。

## 增量回填

- **已完成**：下载器按流实际字节限制，唯一临时文件、校验后替换、失败原文件保留；PNG内容与扩展名一致；目录坏JSON/越界/重复ID报错，写锁、逐项原子目录检查点和新增/复用/修复计数。新增14项聚焦测试，连同既有素材审计共24项通过。Windows libvips保留文件锁反例已修，使用受限字节缓冲解码再落临时PNG。
- **已完成**：真实搜索只返回许可uid/label，补查询 `/v3/licenses/:uid` 并缓存。实际许可接口返回CC-BY-4.0规范链接后才放行，拒绝名称推断、伪造许可主机、NC/SA及缺作者。下载签名接口另有小批量配额上限，不在无GLB时无限消耗配额。
- **已完成（仅缓存）**：此次确切新增2个GLB，旧164→166；2次下载接口、0错误/无效预览图。机械夹爪 `71df42c5d5964d2ea149c5513bbc061b`，作者trinityscsp，277392字节；工业安全栏 `cda46c9b520c48708dbecb5a72bfdf9a`，作者Just8，4155016字节。均CC-BY-4.0，完整来源/作者/许可链接已记录；旧目录备份 `data/external-assets/source-b/catalog.before-codex-20260905-2028.json`。密钥与开发账号未改。
- **本轮待办**：这2个记录仍为review-required，不能说已经出现在统一素材库。下一步真实浏览器渲染/准确缩略图、source-b目录适配、许可随项目导入和发布保留；现有旧164条需补许可版本/视觉复核，不一键放行。glTF ZIP转换仍待补，不冒充本次已覆盖。

### 统一入库与真实工作流增量（覆盖上条旧状态）

- **已完成**：2项经过实际WebGL渲染和人工目视核对，准确命名为“平行机械夹爪”和“黄黑工业防撞柱”（后者不是整段护栏）。`source-b-render-GvHJ5v`原始几何/材质、干净PNG导出与哈希为审核依据；缓存`audit.json`绑定模型、缩略图哈希与审核结果。只发布这2项，旧164项不放行，原始GLB不改。
- **已完成**：独立source-b目录适配，明确许可、结构证据、实际文件哈希、PNG尺寸、相同模型渲染证据均匹配才进入统一库；缺文件/不匹配不展示，坏JSON或null元数据报错；目录变更自动刷新，失败可重试，无需重启或改存储配置。正常4100 API已200查到两项及作者/CC-BY-4.0。
- **已完成**：模型与材质都校验实际将上传的副本；待审核资源服务端409拒绝；同内容6个并发导入只创建1个记录/任务；署名进入ModelRecord并在素材卡片按需展开，保留原作者/来源/许可链接，不机械删除必要署名。
- **已完成**：资源页/项目与优化器项目可刷新恢复；关闭精选筛选不再排除精选项；迟到导入/项目刷新不会切回旧项目；预览可直接下载无按钮、网格和操控器的真实模型PNG。
- **已完成**：浏览器对比度门禁复现亮色卡片1.1:1，修正同族标题/正文/选项/状态色与主题令牌；卡片之外又发现说明和半透明徽标不足4.5，已修并扩大抽样。`source-b-library-pUMxr4`双主题×1440/980全部通过，文字最低暗5.53/亮4.56，完整证据包括许可、真实PNG哈希、导入转换、原文件不变、重复复用、刷新身份、空态、503重试及跨项目迟到响应。
- **本轮待办**：优化后另存/下载与公开交付的署名继承、新资源回到场景、优化器异步生命周期/职责治理；旧164项补核、ZIP转换、更多有效模型/模板与原六阶段继续。当前API466/Web1210与相邻门禁通过不代表全项目性能或平台对标已完成。

### 优化处理增量（覆盖上条的优化器待办部分）

- **已完成（限定工作流）**：源/结果/Worker/回执独立生命周期、失败输入保留有效结果、重复优化重置保存状态、取消再保存不重复上传；服务端核验同项目源并记录optimization追溯，GLB asset.copyright含作者/许可/修改说明且保留动画。`optimizer-workflow-znjT1z`四组、每组17/17 Worker释放、原文件哈希不变，暗5.53/亮4.56，真实截图已人工查看。
- **已完成（聚焦修复）**：模型名称不带.glb时补扩展名，防止原GLB误作JSON解析；source-a目录不存在不阻断独立source-b，但坏目录仍显式503；原始GLB读取改复用统一资源接口，取消与超时保持有界，不扩大架构例外。类型/全量统一结果以交接§21后续补记为准。
- **本轮待办**：公开署名控件及优化模型真实入场景/发布浏览器扩展刚建立待新build验证；ASSET-002承接素材卡片直接定位模型/场景保存返回新增上下文。当前资源ID与实例ID未分离，不以删除项目模型伪造替换。更多资源/ZIP/旧164审核和完整性能目标均保留。
