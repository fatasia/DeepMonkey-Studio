# 发布打包记录界面验收

本轮把原版本打包任务接入 Studio 与场景管理的发布对话框。任务状态、重试和取消复用 App 服务；重试不创建新发布版本。

## 实际浏览器证据

- 独立项目：`QA D05 发布重试 0915`，项目 ID `4099aab5-8297-4ae4-b3cf-7afb3b9599c9`；场景 `D05 原版本打包验证`，ID `96b1d58a-d8f8-477c-a1a3-5bd4e8e9f63d`。保留用于续验，未删除既有数据。
- Studio 发布 Three WebView 后显示文件名并关闭对话框；重新打开显示“已生成 / 版本 1”。再次下载后服务端版本历史仍只有 v1。
- 切到场景管理，原任务可见；整页刷新后仍恢复同一版本记录，验证了真实 IndexedDB 终态读取。
- 内置浏览器的 download 事件等待 15 秒超时，默认 Downloads 中未找到该文件。当前只证明应用生成状态与记录恢复，文件落盘及包内容检查仍待办。
- 两轮截图覆盖深色、浅色和 1440×900、980×768；截图在本会话内联输出。首轮发现通用 `.dialog` 后加载覆盖发布弹窗宽度，440px 下交付目标名称截断；改为 `.dialog.publication-dialog` 后恢复既定 540px，双主题宽窄窗复检名称完整、底部按钮可见。
- 外观临时切浅色后已恢复深色，原品牌色保留。验收中出现短暂 HTTP 502，运行管理器确认 API/Web 健康；点击重新读取后目录恢复。未将该阶段声明为控制台全程无错误。

## 交互与同族检查

最终当前共享树：Web 默认 worker 全量 2879 项通过、2 项跳过；生产构建含类型检查和包预算、repository gate 通过。期间 Native 导出测试的初始化超时由对应窗口修复为准备阶段加载真实模块，并隔离迟到任务；未延长默认单例超时或跳过测试。

事件回归覆盖同 tick 重复发布、等待中选项/遮罩/Escape 冻结、失败重试、父级 busy 时任务取消仍可达；任务组件覆盖读取失败、重试/取消、目标过滤、重复点击和迟到响应。两入口共用父对话框，宽度修复同时生效。错误正文置于滚动区，不占用固定底栏。

实际浏览器已检查成功、空态、终态刷新、两入口与双主题；5xx 打包失败、执行中取消、刷新中断和更多记录折叠的真实浏览器路径尚未全部验收。跨标签页任务竞争仍待实现。

## 设计复核

按 `design-taste-digitaltwin` 技能 执行，采用其中西门子工业界面的克制信息层级；颜色取自 `styles/base.css`，未新增颜色令牌。以下分数仅针对本轮发布弹窗，不能替代 D01 或完整三维画质验收。

| 维度 | 自评 | 证据或待办 |
|---|---:|---|
| 布局构图 | 9 | 宽度覆盖修正后，双尺寸按钮完整 |
| 令牌一致性 | 9 | 状态使用既有语义色，深浅主题均实测 |
| 排版 | 9 | 目标名称完整，版本数字采用等宽数字 |
| 交互状态完备 | 8 | 事件回归通过，执行中取消/失败浏览器路径待验 |
| 动效质量 | 8 | 遵守 reduced-motion，运行中动画实测待验 |
| 三维渲染质量 | 不适用 | 本轮只改发布 UI |
| 信息设计 | 9 | 版本、目标、状态与操作同列，旧记录可折叠 |
| 反馈即时性 | 8 | 同步阻止重复请求，下载实际落盘仍未确认 |
| 响应式与主题 | 9 | 双主题 1440/980 截图通过 |
| 语义与文案 | 9 | 状态文字/图标/颜色并用，错误不截断 |

本轮未达到完整 Kimi-95 验收出口；D05 保持本轮待办。下一步补真实取消、失败、中断恢复与下载落盘证据。

## 跨标签页互斥补充验证

2026-09-15 19:54–19:56，在同源 IAB 的两个管理页打开上述 QA 场景发布对话框，同时点击 v1 Three WebView 的“再次下载”。两轮均观察到一页完成，另一页提示场景已有打包任务；错误页点击“重新读取”后回到已生成记录。错误态截图中提示、恢复按钮与底栏完整可见。

本轮没有观察到执行中的取消或刷新中断：空场景打包在状态快照返回前已结束。文件落盘仍未确认。控制台仅保留冷刷新前 11:52:42 UTC 的 Vite HMR reload 错误，本轮交互未出现新错误。

Web 默认全量 547 文件通过、2 跳过，2902 项通过、2 跳过；生产构建、包预算、独立类型检查、source-size 与 repository gate 均通过。真实竞争验证不替代 Native 运行与完整 D05 验收。

## Native 正式交付失败补充验证

2026-09-15 20:03–20:07，独立QA场景新增v2/v3。冷刷新后原v2 Native重试显示失败及对象路径、原因、替代方式；随后新发布v3的打包失败保留弹窗，v3仍为当前发布，历史v1–v3均保留。展开“更多记录（1）”可见v1 Three WebView按钮；长错误内容在弹窗内部滚动，底栏可见。

首次开发HMR尚保留旧App runner的导出函数引用，v2一度按旧逻辑显示已生成；冷刷新后才验收新门禁。本轮未将开发热更新状态作为正式阻断成功证据。正式入口真实编译集成测试确认无ZIP创建或下载调用；浏览器文件落盘、执行中取消和中断恢复仍未验证。

当前Web全量2946项通过、2跳过；生产build、typecheck、repository gate、source-size通过。Native兼容预检仍需前移到publish前，当前失败验收只证明发布版本与打包状态独立。

补记：整页刷新后，v3 failed、具体错误与重试按钮均从真实IndexedDB恢复。冷刷新后交互无新控制台错误，日志保留此前12:02:48 UTC的HMR错误。

## Native 发布前检查补充验证

2026-09-15 20:19–20:23，管理页与三维编辑器的Native发布均在创建发布版本前被兼容检查阻断，弹窗role=alert显示具体原因；两入口截图中的错误区域可滚动，取消与发布底栏可见。管理页历史仍为v3当前发布及v1/v2，没有新增版本。

首次验收发现controller报告错误后返回false，父弹窗没有取得本次失败原因。修复为有效请求错误透传后再次冷刷新，两个入口均显示错误；陈旧场景请求仍静默返回，正常取消不抛异常。完整Web2989项通过、2跳过，生产构建与仓库门禁通过。

验收期间API多次短暂502，日志记录4100端口拒绝连接，后续运行管理器健康。恢复后继续验收，未重启共享服务。真实Native成功发布仍受窗口证据和完整能力覆盖限制，本记录不代表D02/D03整体完成。
