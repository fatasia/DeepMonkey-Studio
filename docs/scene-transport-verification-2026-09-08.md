# 场景单车搬运验证 · 2026-09-08

## 已完成

本批限定流程：场景对象绑定“搬运（单车）”→编辑搬运时长及等待容量→显式连线→保存刷新→原 DES 正式 Study→统一时间线连续回放、暂停、倍速、定位→原快照复现→关闭清理。

根因：原时间线已有运输时段插值，但 3D 覆盖层只取当前节点锚点；场景快速作者器也没有搬运角色。本次接入既有 `transport` 与单车资源，传递真实运输起止时刻及实际后续入站节点；不是新增调度或物理算法。分流不猜第一条边，缺完成/后继事件不补造行程。原场景对象变换与保存内容不写入覆盖层。

- 聚焦：`scenePlantModel`、`plantLitePlaybackModel`、`scenePlantPlaybackOverlay` 共 3 文件 16 测试通过；包括原 DES 运行复现、分流目标、截断证据、等待出站、三轴插值、反向定位、固定缓冲区与清理。Web typecheck 通过。
- 真实浏览器：`node apps/web/scripts/gate-scene-plant-flow.mjs --transport`，r37 两轮 dark1440/light980 共 **4/4**，退出 0。
- 产物：`test-output/codex-2026-09-05/scene-transport-flow-ClVqot/report.json`；两轮参数与 1/4、3/4 行程截图。已亲审两轮 6 张图。
- r37 Web index SHA256：`689d51d6a3748db352700a8d9ce284cedd8bf59771f7791402f6036f24732802`。根构建日志：`.runtime-logs/codex-20260908-unified-build-r37.log`。
- 浏览器实际断言：配置保存恢复；原正式 Study；单一播放器；两定位时刻 3D 画布像素不同，倒退恢复完全相同像素；4×按钮状态及时间推进；暂停时钟稳定；复现输入指纹、轨迹和统计完全一致；关闭时间线/面板后清理；原变换保持且无 helper 持久化。
- 产品错误 0。首轮 dark1440 有一条既有显卡着色器 X4122 精度警告，其他三组为 0；不称整个控制台无警告。抽样正文对比度最低暗 7.57、亮 7.52。

## 已完成：r38 修补与 r39 最终复验

r37 截图发现“等待队列容量”输入框聚焦后底部约 8px 超出滚动口。根因是滚动容器没有焦点留白，输入光标可见并不代表控件整框可见。

已补 `.scene-simulation-body` 的 `scroll-padding-block:12px` 和表单控件 `scroll-margin-block:8px`，门禁新增聚焦后整框上下边界断言。r38 四组合均有整框通过证据；最终统一构建 **r39** 再跑两轮四组 `scene-transport-flow-GGpMJq` **4/4 退出 0**，完整运行链与整框断言一起通过，最终两轮参数/回放截图已再次亲审。未修改冻结 dist。

- r38 index SHA256：`dcf6249a8229ccf06ffd52aefa1678edf17e23ca04d5e7b45e95fce44e24338e`。
- r39 index SHA256：`2aa556cda508c7f245e4fa804e08d82678c4f281833bc59845d19325735e22e3`。构建日志完成时间 12:02:04，最终门禁开始 12:03:07，因此最终证据归 r39，不混记为 r38。
- 聚焦增量含面板布局：4 文件 20 项通过；门禁 `node --check` 通过。

### 保留的像素门禁反例与修正

原整画布 PNG 哈希断言混合了编码、GPU 舍入、未稳定渲染三个因素；未删失败记录，未通过放宽到“看起来差不多”掩盖问题。

| 目录 | 结果与定位 |
|---|---|
| `scene-transport-flow-9EpB4D` | r38 首轮两组过，第二轮深色 PNG 哈希不同；当时中间 canvas 缓冲未保存，不能断言具体原因。随后的两张整页图视口逐像素差为 0，不足以证明前一帧也相同。 |
| `scene-transport-flow-gvK2vm` | 加 canvas 原图存证后四组哈希门禁过；保留为过渡证据，不据此忽略间歇失败。 |
| `scene-transport-flow-LmTYmo` | 解码 RGBA 零容差比较在浅色失败：445560 像素中仅 2 像素变动，最大通道差 1/255，位置在背景网格，运输标记未变。 |
| `scene-transport-flow-AYir5h` | 明确微小 GPU 噪声阈值仍拦住深色反例：841824 像素中 78 像素、最大通道差 65，分布在网格/箱体边缘；没有继续扩大阈值放行。 |
| `scene-transport-flow-GGpMJq` | 取证前等待连续三次渲染稳定后最终四组通过；所有倒退画面实际为 **0 变化像素、最大差 0**，未消耗噪声容差。 |

门禁仅增加取证就绪条件：最多 10 次、每次 100ms 间隔检查连续三次截图稳定；未稳定即失败，不修改相机、阻尼或产品状态。恢复像素允许边界保持为最大通道差 ≤1 且变化像素 ≤总量 0.001%；实际位移则要求 ≥40 变化像素且最大差 ≥8。三轴空间与时刻纯逻辑测试仍精确比较，浏览器倒退后的 2D 坐标字符串仍严格相同。未读取相机内部数值，故“相机阻尼是唯一原因”仍属未证实推断，不写成产品根因已定论。

最终解码像素统计（原始 PNG 均保留）：

| 两轮各组 | 1/4→3/4 变化像素 / 最大通道差 | 倒退恢复变化像素 / 最大通道差 |
|---|---:|---:|
| dark1440 第 1、2 轮 | 2260 / 217 | 0 / 0 |
| light980 第 1、2 轮 | 2066 / 232 | 0 / 0 |

## 本轮待办

还需统一收尾：原时间线长说明与端点文字截断、旧青色动点与语义色令牌、980 工具坞挤压；本批不扩改这些视觉模块。关闭后历史恢复、空间热力、跨域轨道及机器人/PLC/What-if 深化仍按总账推进。

## 对标与视觉自评

依据 [SIM-001 单工作台与时间线方案](specs/SIM-001-simulation-in-editor-design.md) 的西门子单工作台、统一播放和 Study 证据原则；沿用 FVS 等比视图与既有主题令牌，不声称已达到其完整工业能力。按 `design-taste-digitaltwin` 执行两轮截图亲审与同族滚动排查。

| 维度 | 自评分 / 10 | 证据与不足 |
|---|---:|---|
| 布局构图 | 8.5 | 自动收起让位有效，r39 表单焦点底部裁切已修；窄窗整体工具区仍需收尾 |
| 令牌一致性 | 8 | 主操作跟随金色；旧回放动点仍青色 |
| 排版 | 8.5 | 表单可读；窄时间线说明和端点文字截断 |
| 交互状态 | 9 | 真实运行、暂停、定位、倍速、清理通过；不扩大到全部故障态 |
| 动效质量 | 9 | 同一时钟驱动运输插值；反向定位无累积漂移 |
| 3D 渲染质量 | 8 | 标记可辨；旧网格较密，简单方块夹具不代表工业资产画质 |
| 信息设计 | 8.5 | 时长/容量与存证明确；长 Study 信息仍可精简 |
| 反馈即时性 | 9 | 运行状态、时间及暂停结果可见 |
| 响应式与主题 | 8.5 | 两轮双主题通过；980 工具坞仍待整体收尾 |
| 语义与文案 | 9 | 明示单车与轨迹样本，不冒充真机或物理交通 |

存在低于 9 分维度，不宣称通过全套 Kimi-95 或全项目视觉验收。

## 明确排除

本批不实现物理车辆动力学、道路交通避障、认证仿真、OLP、控制器矩阵或新仿真算法；不改账号、普通服务拓扑或原数据，不 push。

## 项目级后验收

大规模网络、真实工业资产、任意客户参数与跨域正确性需后续整体证据。本批单车固定容量 1，不代表完整车队容量/故障配置已接入场景快速作者器。
