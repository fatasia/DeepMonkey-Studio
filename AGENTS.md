# 项目工程与产品验收规则

## 多会话并行协作纪律（2026-09-04 用户指令，所有会话生效）

- 每次任务开始前必须先 review 既有改动：`git log` 近期提交 + `git status` + 总账与 `docs/specs/` 回填；用户会并行让 GPT/Codex 等其它会话做任务，**绝不重复建设**，发现未记录的新提交先读 diff 理解再动手。
- **绝不修改**用户名/密码（admin/admin 固定）与数据库连接/.env 存储拓扑（postgres+minio 权威）。
- 特别大或有风险的改动（跨模块架构重构、鉴权/存储/发布语义变更、删除既有能力、新依赖引入、公共合同大改）必须先经用户同意，记录候选方案等批准，不得擅自实施。
  - 2026-09-05 用户明确更新：上述事前批准要求仅针对 GLM；Codex/GPT 接手本轮可直接实施任务范围内改进，无需逐项询问。严禁 push、固定 admin/admin、存储拓扑不变、保留既有数据等约束不变；不代表授权无关的外部操作。
  - 2026-09-13 用户明确更新：Codex 可在功能切片完成且门禁通过后自主管理 commit 与 push，无需逐次询问；仍须先审查并保留并行会话改动，禁止强推，固定 admin/admin、存储拓扑与既有数据约束不变。
  - 2026-09-13 Deep Engine 范围收敛：删除完整 Three 插件逐个兼容、复杂 Shader Graph 产品、非 Windows 平台适配、重型光追/路径追踪、UE 全套虚拟几何和长尾格式新解码器。保留高频 Three 桥、轻量 Shader、Nanite Lite 与 glTF/GLB 深度支持。新增前必须先核对依赖版本、标准/官方能力和本地实现；能复用或接线就不重复实现。
- 代码质量、交互 UI 一致性、连贯性、用户体验、审美全部必须过关：改动前先看相邻页面既有模式与设计令牌；收尾自查双主题、双分辨率、空/错误态、键盘可达与控件命名；拿不准按"更克制、更统一"处理。
- 2026-09-06 用户长期更新：界面不堆叠多余说明、装饰标识或徽章；只保留必要名称、操作和真实状态，补充说明放悬浮提示或按需展开。交付与进度汇报保持简短；必要错误和操作结果不可隐去。
- **测试深度必须特别深，禁止浅尝**（2026-09-04 用户指令）：除主路径外覆盖边界与失败路径（空态/极值/980px 窄窗/断网/5xx/401 二次复核/重复提交/并发保存/刷新恢复）；交互元素逐个真实点击遍历，断言实际行为而非仅 DOM 存在；触碰公共样式/组件必须跑 Web 全量与相关浏览器门禁；数据链路走全程（保存→刷新→恢复→发布→公开读取）；控制台 0 错误 0 产品可控警告；性能不回退；发现一个缺陷必须排查同族缺陷（弹层裁切→全局查 overflow/z-index，点击无效→遍历同类入口），根因面修复而非单点补丁。

## 代码质量硬门槛

- 新增能力前必须先核对当前依赖版本、官方能力、本地已有实现、路由、测试和真实样本证据，先区分“已有但未接入/未配置”和“确实缺失”。已有官方或本地实现满足需求时直接复用，只补真实缺口，禁止基于旧认知重复造轮子。
- 所有新增或修改代码必须可读、可维护、命名明确，并符合所在模块已有规范。
- 注释用于解释业务约束、关键取舍和不直观的边界条件；必要说明使用简洁中文，不做逐行翻译式注释。
- 新功能在编码前就按“合同/纯逻辑/运行适配/状态编排/页面呈现/测试夹具”划分职责，禁止先堆成大文件再为门禁机械拆分。
- 新增业务文件原则上控制在 300 行以内，复杂页面或控制器尽量不超过 400 行；单个函数超过约 80 行时必须检查是否混入多项职责。500 行是必须优先拆分的预警线，800 行是绝对阻断线而不是设计目标。
- 修改既有超大文件时，不继续堆叠职责，并在安全范围内按稳定业务边界逐步抽取；拆出的模块必须有清晰输入输出，不能只搬运代码行。
- 优先使用直接、自然、便于接手维护的实现，避免模板化冗余、无收益抽象、炫技写法和 AI 式重复代码。
- 完成前必须运行与改动最相关的类型检查和测试，并复查错误处理、取消、超时和降级路径。

## 工业格式硬门槛（2026-09-17 用户指令，覆盖旧范围冲突）

- `docs/specs/industrial-3d-format-work-plan-2026-09-16.md` 是工业格式唯一权威计划入口。任务恢复、估时、架构、实现与验收必须先读取并遵守；旧决策或历史 Provider 文档与其冲突时不得沿用。
- Parasolid 文本格式的用户可见名称必须写 **`X_T`**，文件扩展名写 **`.x_t`**。禁止写 `XT`；仅历史文件名、URL slug、稳定机器 ID 或不可改外部符号可以保留该字符序列，且不得映射成用户可见名称。
- 生产路线限定为**自研/开源、内置、本地离线**：不得依赖、建议或回退到外部商业 SDK、商业转换器、源 CAD/BIM 软件、云转换、在线授权或许可证服务器；这些项也不得被列为阻塞条件或完成工期前提。
- 允许引入经过逐文件授权审计、固定版本、可复现构建并随安装包交付的开源解析器、压缩库和几何库。其许可证只作为第三方分发治理，不是商业许可依赖。
- 自研/开源链路证据不足时，能力只能保持 `inspect`/`preview` 或明确阻断；严禁使用商业链路产物、文件头、包围盒或合成三角形冒充 `visual-complete`/`engineering-verified`。
- 可从 `D:/Download` 与网络开放来源补真实样本。样本进入仓库或固定语料前必须记录来源、SHA-256、授权/使用边界、格式版本和预期对象；客户或私有模型只允许本地验证，禁止提交。
- 缺少样本、源码、构建依赖、工具或规范时，禁止未经行动就标记“外部阻塞”。必须先检查 `D:/Download`、仓库与本机缓存、上游官方仓库/发布页、开放数据源和等价开源实现；任务范围内可安全取得的资源直接下载、固定版本并记录 SHA-256。仅当实际检索、下载、构建或验证失败后，才允许登记精确失败项、已尝试来源与下一可执行动作。

## 全面测试与极致优化最终验收

- 全部核心功能任务完成后，统一进行代码、功能、性能、稳定性、全站产品体验和 UI 打磨；该阶段是一个完整交付门禁，不拆分收尾。
- 以完整用户任务流审视页面与交互：入口、配置、执行、反馈、结果、追溯和再次操作必须连贯。
- 统一信息架构、设计令牌、组件状态和交互语言；覆盖加载、空白、错误、无权限、部分成功、离线和响应式布局。
- 单个页面可用但跨页面割裂、状态不可预测、操作无反馈或视觉规范不一致，均视为未完成。
- 最终统一执行 E2E、视觉回归、性能基准、长稳、故障注入、可访问性与发布回滚，并修复发现的问题后再验收。
- 最终阶段对全仓执行文件与函数体量扫描；职责过载的超大文件必须按领域、状态所有权和运行边界拆分，不能只检查新增文件，也不能为了行数机械切片。
- 超大文件拆分前后必须做行为回归、视觉对比和性能对比；只有结构更清晰且功能、体验、性能不退化才算完成。

## 2026-08-31 最终任务范围

- 2026-09-04 范围更新：用户开启"全面超越"新开发展轮，一级载体为 2D/3D/脚本编辑器与数据中心，二级为 AI 与西门子 Lite 仿真；能力项、批次与验收以 `docs/platform-surpass-development-plan-2026-09-04.md` 为准。该轮取代本节"不再扩张功能域"的旧约束；下节各项硬门槛与 §28"明确不再执行"排除清单继续全部有效。夜间体验优化轮（`docs/nightly-extreme-optimization-plan-2026-09-04.md`）并行继续。
- 以 `docs/product-depth-experience-stability-ai-mcp-upgrade-plan-2026-08-27.md` 第 28 节为唯一剩余任务总表；除用户明确加入的 Plant Lite、PS Lite、PD Lite 与自研格式探测外，不再扩张功能域。
- Plant Lite、PS Lite、PD Lite 只抽取高价值工程闭环，不复制完整西门子套件，不新增一级导航，不建设完整 OLP、完整 PLM、认证级人体工效或厂商控制器矩阵。
- 工业格式采用分级能力与证据合同：允许实现安全结构探测和经过样本验证的高价值子集；没有几何、拓扑和版本矩阵证据时，不得标记为可渲染或生产可用。
- “本轮不做工业格式真实转换验收”是旧范围，已被 2026-09-17 ACTIVE GOAL 与上方工业格式硬门槛覆盖；当前按权威工业格式计划推进全部可执行任务。不补 PINO、PINN、TwinMoE 生产证据，不执行电池模型正式上线。
- 上述能力已有代码、路由和测试必须保持不退化；“不做”表示退出本轮交付与验收目标，不表示删除现有实现。
- 用户已取消 WebGPU 8 小时长稳；保留短时资源、画质、设备丢失和恢复门禁，但不把 8 小时 soak 作为完成条件。
- 后续新增想法只记录为本轮外候选，不得打断当前任务闭环、全量测试与文档交付。

## 任务恢复与上下文连续性

- 任务续跑、服务恢复或上下文压缩后，必须先阅读 `docs/active-task-recovery-ledger.md`、本文件和总计划，再查看 `git status`、最近提交及门禁产物；禁止仅凭聊天摘要重新定义范围。
- `docs/active-task-recovery-ledger.md` 是当前冻结任务的恢复入口。范围、完成状态、排除项或权威验证证据发生变化时，应在同一批提交中更新总账。
- 原始会话日志不得提交到仓库。总账只保留经核验的用户决策、实现状态、证据和待办，不记录凭据、临时密码、内部推理或大段工具输出。
- 旧计划中的“待建”“继续补齐”不能自动成为当前待办；必须先与总账中的后续用户决策、明确排除项和当前代码证据交叉核对。
- 每次交接只允许使用四类状态：`已完成`、`本轮待办`、`明确排除`、`项目级后验收`。不得把明确排除项重新包装为本轮缺口，也不得把固定夹具验证扩大成任意客户项目承诺。

## 公开源码治理与仓库门禁

- 本仓库的唯一项目许可证为根目录 `LICENSE` 中的 `Deep Monkey Community Source License 1.0`，SPDX 自定义标识为 `LicenseRef-Deep-Monkey-Community-1.0`；第三方组件继续按其各自许可证和 `THIRD_PARTY_NOTICES.md` 管理。
- 该许可证按企业行为设置使用条件，因此项目对外统一称“公开源码”或“source-available”，不得标注为 OSI Approved、OSI Open Source、Free Software 或使用会造成同等误解的徽章和宣传语。
- 任何企业实施 `LICENSE` 定义的 Covered Misconduct 时即成为受限组织；未实施这些行为的企业以及个人按普通非受限用户处理。
- 受限组织不得以任何方式使用本项目，包括内部使用、评估、测试和研究，也不得通过代理、关联企业、承包商、客户、供应商或其他第三方使用。公开源码、支付费用或商业买断均不构成例外。
- 许可证正文、受限主体、商业使用定义或公开源码义务属于项目根本政策，只有用户明确授权才能修改；任何修改必须同时更新 `LICENSE.zh-CN.md`、`LICENSING.md`、`README.md`、本节、门禁断言和变更日志。
- 每个 Pull Request 必须说明问题、范围、验证证据、文档影响和许可证/第三方依赖影响。用户可感知行为、公共合同、部署方式或治理规则发生变化时，同一 Pull Request 必须更新相应文档和 `CHANGELOG.md` 的 `Unreleased`。
- 合并前必须运行 `pnpm gate:repository` 以及改动范围对应的类型检查、单元测试、构建和专项门禁；默认分支应启用保护规则，禁止绕过必需检查和强制推送。发布只从受保护分支产生，使用 SemVer 标签并从 `CHANGELOG.md` 生成发布说明。
- 禁止提交 `.env`、密钥、证书、令牌、生产数据、客户模型、运行日志和登录截图。疑似漏洞或凭据泄露按 `SECURITY.md` 私下报告，禁止先在公开 Issue 披露。
- 外部贡献必须有权提交，并同意其贡献按项目许可证发布；引入或更新依赖、模型、字体、图标、样例资产时，必须核对再分发权、许可证兼容性、来源和必要署名，并同步 `THIRD_PARTY_NOTICES.md`。
- `LICENSE`、`LICENSING.md`、`CONTRIBUTING.md`、`CODE_OF_CONDUCT.md`、`SECURITY.md`、`GOVERNANCE.md`、`CHANGELOG.md`、GitHub 模板及仓库门禁脚本均为受保护治理文件，不得为让 CI 变绿而删减必需条款或跳过检查。
